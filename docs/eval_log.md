# Evaluation log

Running record of every experiment run for the iteration loop described in
`docs/roadmap.md`. One row per run. Numbers only — if you want the reasoning
behind a change, that's in `roadmap.md` or the relevant commit, not here.

Metrics follow spec §8.2: Recall@1/5/10, MRR, median/mean rank. Caption
retrieval runs (SigLIP2) also report num_candidate_captions since R@5/R@10
are only meaningful relative to how many candidates exist.

| Date | Phase | Model | Dataset scope | R@1 | R@5 | R@10 | MRR | Median rank | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 2026-07-27 | Phase 1 smoke test | finetuned (srpone/zooclaw-fashionsiglip2) | 12-product local sample, 58 images, 7 candidate captions | 75.86% | 100% | 100% | 87.36% | 1.0 | Pipeline validation only, not a real baseline — too few candidates for R@5/10 to be meaningful |
| 2026-07-27 | Phase 1 smoke test | base (google/siglip2-base-patch16-384) | 12-product local sample, 58 images, 7 candidate captions | 68.97% | 100% | 100% | 80.80% | 1.0 | Same caveat — pipeline check, finetuned > base directionally sane |
| 2026-07-28 | Phase 1 real baseline | finetuned (srpone/zooclaw-fashionsiglip2) | full 1115-product / 6-brand catalog, 4334 images, 561 candidate captions | 7.89% | 21.87% | 31.15% | 16.09% | 26.0 | Real baseline — R@1 much lower than the smoke test's 76%, expected since 561 real candidates vs. 7 toy ones. This is the number that matters. |
| 2026-07-28 | Phase 1 real baseline | base (google/siglip2-base-patch16-384) | full 1115-product / 6-brand catalog, 4334 images, 561 candidate captions | 7.15% | 18.39% | 25.73% | 13.69% | 40.0 | Finetuned beats base on every metric (R@1 +0.7pt, R@5 +3.5pt, R@10 +5.4pt, MRR +2.4pt, median rank 26 vs 40) — the finetune is real, not noise, but overall retrieval is still weak in absolute terms (R@1 under 8%) with 561 candidates. Confirms Phase 1's own conclusion: worth re-training on the full corpus (roadmap Phase 1 next step), current finetune was only ever trained on the old 2-brand shoe set. |
| 2026-07-29 | Phase 1 full-corpus retrain (v2) | finetuned SigLIP2 (`finetune_siglip2.py`, two-stage: heads then last-block, structured_caption-only, no SKU text, category-scoped hard-negative batching) | full 1234-product / 6-brand / 13-category catalog, exact-label test split, category-scoped candidate pool | 14.04% | 36.83% | 53.86% | 26.40% | 9.0 (mean 15.26) | First real full-corpus retrain, run directly (not Modal). Nearly 2x the prior baseline's R@1 (14.04% vs 7.89%) and a much tighter median rank (9 vs 26) — the finetune-on-full-corpus hypothesis from the 2026-07-28 baseline row is confirmed. This is now the number `finetune_siglip2_v3.py` is trying to beat. |
| 2026-07-29 | Phase 1 v3 improvement round | finetuned SigLIP2 (`finetune_siglip2_v3.py` on Modal, A10G: last-4-block unfreeze w/ layer-wise LR decay, offline mined hard negatives, all-positive-caption sampling, sigmoid loss, K=4/batch 64) | full 1234-product catalog, exact-label test split, global candidate pool | 15.83% | 43.47% | 57.99% | 29.33% | 8.0 (mean 17.54) | Global-pool number, for direct comparison to the 2026-07-28 baseline row (which was global-pool only). |
| 2026-07-29 | Phase 1 v3 improvement round | finetuned SigLIP2 (`finetune_siglip2_v3.py` on Modal, A10G, same config as above) | full 1234-product catalog, exact-label test split, category-scoped candidate pool | 18.83% | 51.09% | 67.59% | 34.12% | 5.0 (mean 10.94) | Beats v2 on every metric (R@1 +4.8pt, R@5 +14.3pt, R@10 +13.7pt, MRR +7.7pt, median rank 5 vs 9). Selected checkpoint: stage2_lastnblocks (val R@1 14.79% vs stage1's 10.51%). Ablations not yet isolated (sigmoid loss only was tried; InfoNCE and per-lever contributions untested) but the combined v3 change set is a clear win over v2. |

| 2026-07-29 | Phase 1 v3, by label kind | finetuned SigLIP2 (`finetune_siglip2_v3.py` checkpoint, stage2_lastnblocks_best) via `evaluate_siglip2_by_label_kind_modal_body.py` | full 1234-product catalog, same 2,059-image held-out test split as the row above, kind=**generic** (category, 276 candidates) | 95.09% | 100% | 100% | 97.34% | 1.0 | Breaks the 18.83% exact_label number down by label kind, per user request ("18.83% seems low, captions are simple/feature-oriented — what's the R@1 per kind"). `exact_label` in the training script only ever evaluates the "model" kind below — this row shows coarse category classification is essentially solved. |
| 2026-07-29 | Phase 1 v3, by label kind | same checkpoint | same split, kind=**brand** (e.g. "Nike sneaker", 581 candidates) | 61.39% | 91.26% | 97.38% | 74.07% | 1.0 | Solid. |
| 2026-07-29 | Phase 1 v3, by label kind | same checkpoint | same split, kind=**attribute** (e.g. "black leather sneaker", 2,633 candidates) | 27.20% | 68.53% | 84.36% | 45.18% | 3.0 | Weak — large fine-grained candidate space, and some attribute facets (material especially) may not be strongly visually grounded, so part of this could be a real ceiling rather than pure undertraining. |
| 2026-07-29 | Phase 1 v3, by label kind | same checkpoint | same split, kind=**model** (specific product line, 1,264 candidates) | 17.68% | 45.90% | 61.15% | 31.41% | 6.0 | This is what the 18.83% headline number actually measures — the hardest tier by design (one step below full SKU, which is deliberately DINOv3's job, not SigLIP2's). Weakest of the four, and the one dragging the aggregate down. |

| 2026-07-29 | Phase 3 step 2 | frozen DINOv3 backbone (raw pooled features, no head) | full 1234-product catalog, exact-SKU validation split | 25.24% | 41.39% | 50.75% | 33.36% | 10.0 (mean 140.62) | Frozen baseline, printed before stage 1 training — 16.3% of wrong top-1s were same-model/different-colorway (the "reasonable" confusion), 83.7% unrelated item. |
| 2026-07-29 | Phase 3 step 2 | DINOv3 identity fine-tune, stage1_head (`dino_identity_finetune.py`, SupCon loss, P×K=8×4 identity-balanced batches, colorway-sibling hard negatives) — epoch 10/10, heads-only | full 1234-product catalog, exact-SKU validation split | 56.84% | 76.35% | 84.82% | 65.79% | 1.0 (mean 21.14) | Huge jump over the frozen baseline (R@1 +31.6pt) from training a projection head alone. 9.8% of wrong top-1s same-model/different-colorway vs. 90.2% unrelated — the ambiguous-confusion share barely moved, meaning the gains are from real discrimination, not just getting better at the easy case. Stage 2 (last-block unfreeze) crashed immediately after on a real bug (see roadmap) — this is the stage1 checkpoint's number, not a final test-set result yet. |
| 2026-07-30 | Phase 3 step 2 | DINOv3 identity fine-tune, stage2_lastblock (`dino_identity_finetune.py`, rerun after the `model.model.layer` fix, epoch 6/6 val) | full 1234-product catalog, exact-SKU validation split | 57.28% | 76.96% | 84.91% | 66.20% | 1.0 (mean 20.71) | Only +0.44pt val R@1 over stage1's 56.84% — last-block unfreezing barely moved the needle beyond what head-only training already captured. Selected as final checkpoint anyway per the "beats stage1" rule. |
| 2026-07-30 | Phase 3 step 2 | DINOv3 identity fine-tune, **final held-out test** (stage2_lastblock_best) | full 1234-product catalog, exact-SKU test split | **56.55%** | 76.72% | 84.96% | 65.60% | 1.0 (mean 22.99) | **Phase 3 complete.** Up from a 25.24% frozen baseline (+31.3pt). 13.2% of wrong top-1s are same-model/different-colorway vs. 86.8% unrelated — most remaining errors are real misses, not the "reasonable" confusion case, room left for hard-negative refinement but this is a strong result as-is. |
| 2026-07-30 | Phase 4, first real run | `hierarchical_retrieval_pipeline.py` end-to-end (`--evaluate`) on Modal — SigLIP2 v3 stage2_lastnblocks_best + **frozen (non-fine-tuned) DINOv3** (checkpoint not yet on the Modal Volume, only on Colab Drive), WITH category gate | 1,077-product catalog (some missing images on this Volume), 1,043 held-out queries | 11.03% | 21.48% | 30.39% | 16.95% | 69.0 (mean 542.29) | First real (not mechanical-only) Phase 4 number. Category-gate exclusion rate 27.52% (over a quarter of queries get gated into the wrong category, unrecoverable) — the gate is net-negative, see the row below. Identity-shortlist miss rate 49.66% — the SigLIP2 semantic-narrowing stage (top-10 identity candidates) is the dominant bottleneck, not the gate and not even the DINOv3 rerank. |
| 2026-07-30 | Phase 4, first real run | same run, WITHOUT category gate (fallback comparison) | same split | **14.96%** | 27.61% | 37.97% | 21.88% | 27.0 (mean 458.62) | Beats the gated config on every metric — confirms the gate is currently net-negative, not just noise. Identity-shortlist miss rate still 41.90% even ungated — this is the real bottleneck to attack next (widen `TOP_IDENTITY_CANDIDATES` beyond 10, and/or v3→v4's "model" kind quality, which v4 deliberately trimmed sampling weight on — worth checking this doesn't regress once v4 lands). This whole row is still artificially low from frozen DINOv3 doing the final rerank instead of the real 56.55% fine-tuned checkpoint — rerun once that's pushed to the Modal Volume for the real number. |
| 2026-07-30 | Phase 4, **real run** (both fine-tuned checkpoints) | `hierarchical_retrieval_pipeline.py` end-to-end (`--evaluate`) on Colab — SigLIP2 v3 stage2_lastnblocks_best + **real fine-tuned DINOv3** (56.55%-R@1 checkpoint, pushed from the Modal Volume down to Colab Drive), WITH category gate | full 1234-product catalog, 1,190 held-out queries | 31.34% | 45.80% | 47.98% | 37.39% | 1235.0 (mean 637.15) | Big jump from the frozen-DINOv3 run (R@1 11.03%→31.34%) — DINOv3 fine-tuning was the single biggest lever, as predicted. Category-gate exclusion rate 30.00%, identity-shortlist miss rate 51.51% (>50%, which is why median rank lands on the sentinel value — see next row's note). Median rank at this "just over 50% miss" threshold is a real artifact of the metric, not a new problem. |
| 2026-07-30 | Phase 4, real run | same run, WITHOUT category gate (fallback comparison) | same split | **36.72%** | 53.19% | 56.05% | 43.69% | 4.0 (mean 536.65) | Beats gated on every metric again — gate confirmed net-negative a second time, independent run. Identity-shortlist miss rate 43.36% (just under 50%, hence the low median here vs. 1235 above — same underlying miss-rate distribution, different side of a threshold). **Conditional R@1** (accuracy given the true product IS in the shortlist): with-gate 64.6%, without-gate 64.8% — essentially identical, meaning DINOv3's rerank itself is excellent (~65%) and the category gate has zero effect on rerank quality, only on shortlist coverage (where it still hurts). Confirms the identity-shortlist stage (SigLIP2 top-10 narrowing), not DINOv3, is now the dominant bottleneck by a wide margin — same conclusion as the frozen-DINOv3 run, unchanged in magnitude (43-52% miss both times). |
| 2026-07-31 | Phase 4, **after widening shortlist** | same checkpoints, `TOP_IDENTITY_CANDIDATES` 10→25 (per the diagnosis above), gate still on by config default at the time of this run (pre-dates the gate-off-by-default code change) | same 1234-product catalog, 1,190 held-out queries, WITH category gate | **39.66%** | 59.75% | 62.77% | 47.67% | 3.0 (mean 436.43) | Category-gate exclusion rate unchanged (30.00%, expected — the gate logic itself didn't change). Identity-shortlist miss rate **35.21%, down from 51.51%** — widening the shortlist worked exactly as predicted. |
| 2026-07-31 | Phase 4, after widening shortlist | same run, WITHOUT category gate | same split | **47.65%** | 72.52% | 76.72% | 57.79% | 2.0 (mean 249.08) | **Best result yet.** Identity-shortlist miss rate **20.00%, down from 43.36%** — more than halved. R@1 up 10.9pt from the pre-widening run (36.72%→47.65%). Conditional R@1 (given shortlist hit) actually dipped slightly, 64.8%→59.6% — makes sense: DINOv3 now discriminates among 25 candidates instead of 10, a harder per-candidate task, but the massive drop in outright misses more than compensates. Net win is unambiguous. |
| 2026-07-30 | Phase 1 v4 improvement round | finetuned SigLIP2 (`finetune_siglip2_v4.py` on Modal, A10G: per-facet `LABEL_KIND_WEIGHTS` — color/fit/closure boosted, "model" kind cut 0.42→0.30 to make room) | full 1234-product catalog, exact-label test split, category-scoped candidate pool | 16.00% | 45.78% | 63.35% | 30.55% | 6.0 (mean 12.46) | **Regression vs. v3 on every metric** (R@1 -2.83pt, R@5 -5.31pt, R@10 -4.24pt, MRR -3.57pt). Confirms the predicted tradeoff: cutting "model" kind's sampling weight to fund color/fit/closure hurt the exact-label task (which *is* essentially the "model" kind) more than expected. `hierarchical_retrieval_pipeline.py`'s checkpoint auto-detection doesn't include v4, so this doesn't affect Phase 4 — still using v3. By-facet breakdown on v4 not yet run, so whether color/fit/closure actually improved enough to justify trying a less aggressive v5 reweighting is still open. |
| 2026-08-01 | Phase 1 v4 by-facet breakdown (the open question above, now answered) | `evaluate_siglip2_v4_by_facet_modal_body.py` on Modal, A10G, vs. the existing v3 by-facet numbers already on the Volume | same 1234-product catalog / split as the v3 by-facet rows above | color 33.85% (v3 31.57%, **+2.29pt**) | material 48.50% (v3 50.08%, -1.58pt) | fit 21.18% (v3 20.05%, **+1.13pt**) | closure 24.09% (v3 23.45%, **+0.64pt**) | **Verdict: not worth it.** The 3 facets v4 specifically boosted (color/fit/closure) all improved, but only marginally — the biggest gain is +2.29pt (color), the other two are ~+1pt or less. `length` unchanged (37.50%→37.50%), `defining_features` and `attribute_caption` both flat/slightly worse. None of this comes close to justifying the exact-label ("model" kind) regression of -2.83pt from the same row above — v4 traded a real, large loss on the hardest/most important task for a thin spread of small gains on secondary facets. Confirms v3 should stay the production checkpoint; if a v5 is attempted, it needs a much gentler "model" kind weight cut than v4's 0.42→0.30, not just abandoning the idea. |

| 2026-08-01 | Phase 4, **real HSC climbing eval** (the question flagged as highest priority since 2026-08-01's HSC-climbing code change) | `hierarchical_retrieval_pipeline.py --evaluate` on Colab — SigLIP2 v3 + real fine-tuned DINOv3, same checkpoints as the 07-31 rows, WITH HSC-based category gate (HSC_THRESHOLD=0.5 default) | full 1234-product catalog, 1,190 held-out queries | 36.72% | 56.22% | 59.41% | 44.62% | 3.0 (mean 474.81) | **HSC did NOT fix the gate — it's now WORSE than the old flat gate on R@1** (36.72% vs. the 07-31 flat gate's 39.66%, -2.94pt), despite genuinely improving the category-gate exclusion rate (25.55% vs. flat gate's 30.00%, a real -4.45pt improvement). Diagnosis from `hsc_climb_level_fractions`: leaf 0.0%, category 13.2%, group 34.9%, root 51.9% — at the default threshold, HSC backs off to NO restriction (root) over half the time and to a broad multi-category "group" restriction another third of the time; only 13.2% of queries get the tight single-category restriction that would actually help the identity-shortlist stage. Isolating just the non-excluded queries confirms the narrowing that DOES land at category level genuinely helps (17.15% shortlist-miss rate vs. ungated's 20% — computed by backing the 25.55%-exclusion queries out of the aggregate 38.32% shortlist-miss rate), but that thin benefit only applies to 13.2% of queries, nowhere near enough to offset the cost of the 25.55% still excluded outright. Root cause is almost certainly the threshold being too conservative for this use case, not the climbing algorithm itself being wrong. |
| 2026-08-01 | Phase 4, real HSC eval | same run, WITHOUT category gate (fallback comparison) | same split | **47.56%** | 72.52% | 76.64% | 57.73% | 2.0 (mean 249.09) | Matches the 07-31 ungated baseline almost exactly (47.65%→47.56%, noise-level), confirming HSC climbing itself changed nothing about the ungated path (expected — HSC only feeds `allowed_categories`, which ungated mode ignores). **Ungated remains the best real number, a third independent confirmation that category gating (flat OR HSC) is net-negative** — `retrieve()`'s gate-off-by-default (set 2026-07-30) is confirmed correct, not just a hedge. If gating is revisited at all, HSC_THRESHOLD needs to be tuned much lower first (to climb to leaf/category more often instead of defaulting to group/root) before it has a chance of beating ungated, and even then it isn't guaranteed to — the identity-shortlist stage may just be strong enough on its own that any exclusion cost outweighs any narrowing benefit. |
| 2026-08-02/03 | Phase 4, **`--top-identity-candidates` sweep past 25** | `hierarchical_retrieval_pipeline.py --evaluate --top-identity-candidates 35` on Colab, same checkpoints, WITH HSC gate | full 1234-product catalog, 1,190 held-out queries | 38.82% | 59.75% | 63.78% | 47.46% | 3.0 (mean 416.92) | Gate exclusion 25.80%, shortlist miss 33.61%. Gated stays well below ungated (see next row) — consistent with the established net-negative-gate conclusion. |
| 2026-08-02/03 | Phase 4, candidates sweep | same run, K=35, WITHOUT gate | same split | **49.08%** | 75.63% | 81.26% | 60.31% | 2.0 (mean 184.06) | Shortlist miss **14.71%, down from 20.00% at K=25.** +1.43pt R@1 over the K=25 baseline. |
| 2026-08-02/03 | Phase 4, candidates sweep | `--top-identity-candidates 50`, WITH gate | same split | 40.08% | 61.68% | 65.71% | 49.01% | 3.0 (mean 380.87) | Gate exclusion 25.80% (unchanged, as expected — HSC prediction doesn't depend on K), shortlist miss 30.67%. |
| 2026-08-02/03 | Phase 4, candidates sweep | K=50, WITHOUT gate | same split | **50.76%** | 78.99% | 85.46% | 62.71% | 1.0 (mean 122.22) | Shortlist miss **9.66%.** Median rank drops to 1.0 for the first time — more than half of held-out queries now get the exact right product as the #1 result. |
| 2026-08-02/03 | Phase 4, candidates sweep | `--top-identity-candidates 75`, WITH gate | same split | 40.59% | 63.19% | 67.56% | 49.96% | 2.0 (mean 352.05) | Gate exclusion 25.80%, shortlist miss 28.32%. Gap to ungated (below) is now ~13pt, WIDER than at K=35 (~10pt) — gating is falling further behind as K grows, not catching up. |
| 2026-08-02/03 | Phase 4, candidates sweep | K=75, WITHOUT gate | same split | **53.53%** | 82.35% | 88.74% | 65.71% | 1.0 (mean 63.54) | **New best real number.** Shortlist miss **4.87%** — essentially solved at this K. Marginal R@1 gain per additional candidate held roughly steady across the whole sweep (25→35: 0.143pt/candidate; 35→50: 0.112pt/candidate; 50→75: 0.111pt/candidate) — **no sign of diminishing returns yet**, worth pushing to K=100+ next. One open, unexplained-with-certainty artifact: `hsc_climb_level_fractions` in this and the two gated rows above (category 13.2%, group 11.0%, root 75.8%, identical across all 3 since K doesn't affect HSC) don't match the 2026-08-01 HSC row's numbers (group 34.9%, root 51.9%) despite HSC being nominally invariant to `top_identity_candidates` — likely explanation is the `evaluate()` batching fix (2026-08-02) changed the floating-point computation path (batched matmul vs. one-image-at-a-time) enough to flip some threshold-boundary HSC decisions; not confirmed by directly diffing internal values, but doesn't affect the core conclusion (gating still clearly net-negative in both the old and new numbers). |
| 2026-08-03 | Phase 4, **`--patch-rerank` real-checkpoint validation** | `hierarchical_retrieval_pipeline.py --evaluate --patch-rerank --top-identity-candidates 50` on Colab, same checkpoints, WITH HSC gate | full 1234-product catalog, 1,190 held-out queries | 13.11% | 40.92% | 65.29% | 25.95% | 7.0 (mean 390.78) | Gate exclusion 25.80%, shortlist miss 31.34%. Compare to K=50 WITH gate, no patch-rerank (row above, R@1 40.08%) — a huge drop, but this row also has the gate confound layered on top; the cleaner comparison is the ungated row below. |
| 2026-08-03 | Phase 4, `--patch-rerank` validation | same run, WITHOUT gate | same split | **20.59%** | 55.71% | **85.46%** | 37.01% | 5.0 (mean 124.07) | **Severe, one-sided regression — patch-rerank is confirmed actively harmful, not just unvalidated.** Compare directly to the K=50 ungated, no-patch-rerank row above: R@1 50.76%→20.59% (**-30.17pt**), R@5 78.99%→55.71% (-23.28pt), but **R@10 is IDENTICAL (85.46% both times)** and median rank only moved 1.0→5.0. This pattern is the real diagnosis, not a coincidence: `patch_rerank()` only re-sorts the top `PATCH_RERANK_CANDIDATES=10` pooled candidates, never changing which items are in that set — so if the true product was already in the pooled top-10, it stays in the top-10 either way (R@10 unaffected), but patch-rerank scrambles the ORDER within that window badly enough to push correct #1 answers down to #5-10. Root cause: DINOv3's `projection_head` was only ever trained on pooled features (never on individual patch tokens, see `embed_image_dino_patches`'s own docstring, flagged as a real caveat before this result existed) -- applying it to raw patch tokens puts them far outside its training distribution, producing near-noise per-patch embeddings that actively destroy an already-good pooled ranking when used to reorder it. **Verdict: do not use `--patch-rerank` under any circumstances as currently implemented** -- this is not "needs more validation," it's a confirmed, severe, one-sided harm. |
| 2026-08-03 | Phase 4, **`--score-fusion` real-checkpoint validation** | `hierarchical_retrieval_pipeline.py --evaluate --score-fusion --top-identity-candidates 50` on Colab, same checkpoints, WITH HSC gate | full 1234-product catalog, 1,190 held-out queries | 34.79% | 60.25% | 66.55% | 45.85% | 3.0 (mean 370.62) | Gate exclusion 25.80%, shortlist miss 29.83%. See the ungated row below for the clean comparison — this one carries the gate confound on top. |
| 2026-08-03 | Phase 4, `--score-fusion` validation | same run, WITHOUT gate | same split | **44.54%** | 76.89% | 85.21% | 58.80% | 2.0 (mean 122.23) | **Score fusion is net-negative and should not be used.** Directly comparable to the K=50 ungated no-fusion row above (50.76%), same K and same gate setting, differing only in the flag: R@1 **50.76% → 44.54%, -6.22pt**, MRR 62.71% → 58.80%. Note shortlist miss is **identical at 9.66%** — fusion happens in `rerank_by_identity`, strictly downstream of the shortlist, so it cannot and does not change coverage; it only reorders. So this is purely a ranking regression, the same shape as `--patch-rerank` though milder. Mechanism is the one already flagged in `USE_SCORE_FUSION`'s own module comment: SigLIP2 scores are **identity-level**, so every colorway sibling of a model gets the SAME score, and blending that into DINOv3's per-product score actively flattens exactly the sibling distinctions DINOv3 was fine-tuned to make. Two of the three "improvement" flags on this pipeline (`--patch-rerank`, `--score-fusion`) are now confirmed harmful against real checkpoints. |
| 2026-08-03 | Phase 4, candidates sweep | `--top-identity-candidates 100`, WITH gate | full 1234-product catalog, 1,190 held-out queries | 42.10% | 64.03% | 69.41% | 51.38% | 2.0 (mean 322.37) | Gate exclusion 25.80%, shortlist miss 25.88%. |
| 2026-08-03 | Phase 4, candidates sweep | K=100, WITHOUT gate | same split | **53.87%** | 83.19% | 90.25% | 66.34% | 1.0 (mean 37.99) | Shortlist miss **2.77%**, down from 4.87% at K=75. But R@1 gained only +0.34pt over K=75 — the marginal return per candidate collapsed from 0.111 pt/candidate (50→75) to **0.0136** (75→100), an ~8x drop. First clear sign of the ceiling the K=75 row said had not been found yet. |
| 2026-08-03 | Phase 4, candidates sweep | `--top-identity-candidates 150`, WITH gate | same split | 41.76% | 63.61% | 69.16% | 51.01% | 2.0 (mean 321.61) | Gate exclusion 25.80%, shortlist miss 25.80% — now exactly equal to the exclusion rate, i.e. **every remaining gated miss is the gate throwing the true category away**, not the shortlist failing. At this K the gate is the only thing still losing candidates. |
| 2026-08-03 | Phase 4, candidates sweep | K=150, WITHOUT gate | same split | **53.95%** | 83.53% | 90.84% | 66.41% | 1.0 (mean 19.11) | **New best real number, and the sweep is now saturated — stop here.** Shortlist miss **1.18%**, essentially zero. Marginal return fell again to **0.0016 pt/candidate** (100→150), another ~8.5x drop; K=150 buys +0.08pt over K=100 for 50% more shortlist. The sweep that began at K=10 is finished: **the identity-shortlist stage is solved and is no longer the bottleneck.** What replaces it is visible in the conditional R@1 (accuracy given the true product IS in the shortlist), which is **flat-to-declining** across the whole sweep — 56.2% at K=50, 56.3% at K=75, 55.4% at K=100, **54.6% at K=150**. DINOv3's rerank gets it right barely over half the time even when handed the right answer, and gets slightly worse as the candidate set grows (a harder per-candidate task). **Every remaining point of R@1 now has to come from the reranker, not from retrieval breadth.** |
| 2026-08-04 | Phase 4, **`GALLERY_IMAGES_PER_PRODUCT` sweep (rerank lever A)** | `hierarchical_retrieval_pipeline.py --evaluate --top-identity-candidates 150` on Colab, `GALLERY_IMAGES_PER_PRODUCT=4`, WITH HSC gate | full 1234-product catalog, 1,190 held-out queries | 45.97% | 65.38% | 70.34% | 54.30% | 2.0 (mean 312.09) | Gate exclusion 25.80%, shortlist miss 25.04%. Gated remains far below ungated; see the ungated rows for the actual result. |
| 2026-08-04 | Phase 4, gallery-cap sweep | `GALLERY_IMAGES_PER_PRODUCT=4`, WITHOUT gate | same split | **58.32%** | 84.62% | 91.60% | 69.47% | 1.0 (mean 18.86) | **+4.37pt over the K=150 baseline (53.95%) for zero retraining.** Each product's DINOv3 prototype is the mean of its gallery embeddings, and that set had been a hardcoded first-2-images; the median product has 5. Index rebuild was incremental as designed — enrolled 946 changed products, reused 288 whose image set was unchanged. |
| 2026-08-04 | Phase 4, gallery-cap sweep | `GALLERY_IMAGES_PER_PRODUCT=6`, WITHOUT gate | same split | **59.50%** | 84.79% | 91.93% | 70.22% | 1.0 (mean 18.80) | +1.18pt over cap=4. Enrolled 589, reused 645. |
| 2026-08-04 | Phase 4, gallery-cap sweep | `GALLERY_IMAGES_PER_PRODUCT=99` (all images), WITHOUT gate | same split | **59.92%** | 84.96% | 92.10% | 70.52% | 1.0 (mean 18.77) | **NEW BEST: 59.92% R@1, +5.97pt over 53.95%, with no retraining and no model change — only which images back each product prototype.** Enrolled 274, reused 960. <br><br>**This is the first thing to actually move the reranker.** Shortlist miss is **identical at 1.18% across all four settings**, which is the proof: the gallery cap is a DINOv3-side change and cannot affect SigLIP2's shortlist, so every point of the gain is rerank quality. Conditional R@1 (accuracy given the true product is in the candidate set) goes **54.6% → 60.6%, +6.0pt** — the metric that had been flat-to-declining through the entire K sweep and was identified as the sole remaining bottleneck. <br><br>Diminishing returns are clear and the curve is steep then flat: **+4.37 (2→4), +1.18 (4→6), +0.42 (6→all)**. Cap 6 captures 93% of the total available gain; going to "all" costs a fuller re-encode for the last 0.42pt. **Recommended default: 6.** <br><br>Interpretation: two views was simply too thin a basis for a prototype that has to separate colorway siblings. The worry that extra views (detail crops, flat lays, back shots) would blur the mean did not materialize at any setting — more angles beat less per-shot noise throughout. Gated improved too (41.76% → 46.89%) but remains ~13pt behind ungated, a sixth confirmation the gate is net-negative. |
| 2026-08-04 | Phase 3, **ArcFace vs SupCon — the benchmark the spec has demanded since day one** | `dino_identity_finetune.py` with `LOSS_TYPE=arcface` (margin 0.30, scale 30.0) on Modal A10G, stage1 10ep + stage2 6ep, **final held-out test split** | Modal Volume catalog, 1,077 identity classes, 4,811 train images (~4.5/class) | **43.14%** | 63.95% | 73.44% | 53.00% | 2.0 | **SupCon wins decisively: 56.55% vs ArcFace's 43.14% on the same test metric, −13.41pt.** This closes a question open since the file was written. <br><br>**It had never once run.** `ArcFaceHead.forward` raised `scatter(): Expected self.dtype to be equal to src.dtype` on the first batch of every attempt: under AMP the `embeddings @ weight.T` matmul returns fp16 so `logits` is fp16, but torch promotes acos/cos to fp32 so `margin_cosine` is fp32, and the scatter_ writing the margin into the target column refuses the mismatch. Fixed by computing the head in fp32 inside an explicit `autocast(enabled=False)` block — also the right precision, since acos is most sensitive as \|cos\|→1, exactly where the margin operates. Note a CPU smoke test passes on the BROKEN code (CPU autocast doesn't promote acos), which is why this survived. <br><br>**Why it loses, and why a margin/scale sweep is NOT the follow-up.** Validation converged flat at ~39.6% across the last six epochs (39.57/39.57/39.57/39.47/39.67), so this is not undertrained. The failure shape is the diagnosis: R@10 **73.44%** and median rank **2.0** mean ArcFace usually gets the right product into the top few and then fails to convert to top-1. ArcFace learns one weight vector per identity; with **1,077 classes at ~4.5 images each** those centroids are badly estimated. SupCon learns no per-class parameters at all — it operates on in-batch pairs — which is far more robust at this data scale. A 13-point gap is not a hyperparameter gap. <br><br>Wrong top-1s were 17.4% same-model/different-colorway vs 82.6% unrelated, essentially matching SupCon's 13.2%/86.8% — so the extra errors are not concentrated in the "reasonable" colorway-confusion case; ArcFace is simply worse at exact identity. Trained on 6,870 of 7,510 images (Volume sync gap), but the frozen baselines differ by only 0.35pt (25.59% here vs 25.24%), so that handicap explains nowhere near 13 points. **Verdict: keep SupCon. Do not pursue ArcFace hyperparameters.** |
| 2026-08-04 | Phase 4/8.1, **open-set rejection calibrated for the first time** | `hierarchical_retrieval_pipeline.py --evaluate --open-set-holdout-fraction 0.1 --top-identity-candidates 150` on Modal A10G, isolated index dir | 70 of 697 identities held FULLY out of the gallery, 601 off-catalog queries with no correct answer at any rank | — | — | — | — | — | **AUROC 0.769 — the DINOv3 score carries real but weak catalog-membership information.** Mean top-1 score: in-catalog 0.9497 vs off-catalog 0.8782. Not the AUROC≈0.5 total-failure case the spec warned about, but nowhere near separable. <br><br>**The operating table is the deliverable, and it is sobering:** p1 threshold 0.8676 → false-reject 1.07% but **false-accept 68.22%**; p10 → 0.9064, FR 10.06%, **FA 55.57%**; best_balanced → 0.9455, **FR 34.48% / FA 26.62%**. There is no good row. To reject even half of off-catalog queries you must throw away ~50% of correct answers. <br><br>**Consequence for the product**: `REJECT_SIMILARITY_THRESHOLD` can now be set from measurement rather than guessed, but any setting is a bad trade. Pointing the Siri surface at a chair will confidently name a jacket roughly two-thirds of the time at any usably-low false-reject rate. This is a **real limitation to design around** (e.g. require a garment detection before querying at all), not a threshold to tune away. A second run in the same log reports AUROC 0.7115 on a different holdout draw — same conclusion, and the spread across draws is itself a caution against over-fitting a threshold to one split. |
| 2026-08-04 | Phase 8, **garment gate re-calibrated against REAL negatives** | `evaluate_garment_gate.py --negatives real`, base `google/siglip2-base-patch16-384`, score = max cosine over 10 garment prompts − max over 13 non-garment prompts | positives 300 `apparel_dataset` product photos + 300 `outfit_dataset` worn outfits; negatives **507 real photos** collected into the new `negatives_dataset/` across 14 themes (furniture, vehicles, food, nature, buildings, interiors, electronics, animals, plants, documents, kitchenware, tools, street, screens) | — | — | — | — | — | **AUROC 0.9994 — real photographs did NOT break the gate, which is the opposite of what this run was set up to show.** Mean margin: apparel **+0.0392**, worn outfits **+0.0388**, real negatives **−0.0545**. The two distributions barely touch: 9.67% of positives fall below the single highest negative, 6.31% of negatives sit above the single lowest positive, and the medians are 0.095 apart. <br><br>**Operating table (round thresholds, because percentile rows are fitted to this exact sample and would move on the next draw):** margin ≥ **0.000** → false-reject 1.33%, false-accept 0.59%; **0.010** → FR 2.50%, FA 0.39%; **0.015** → FR 3.33%, FA 0.39%; **0.020** → FR 5.17%, FA 0.39%; **0.025** → FR 11.83%, FA 0.00%. Youden's-J best-balanced lands at +0.0013 (FR 1.33% / FA 0.59%). **Recommended: +0.010** — 0.39% of real non-garment photos get through, and it sits in the flat part of the curve rather than on a cliff. <br><br>**The false-rejects are entirely one-sided, and that is the finding that matters more than the AUROC.** At every threshold up to 0.010, **100% of false-rejects are worn outfits and 0% are product photos** (0.00% apparel vs 5.00% outfit at 0.010; apparel does not lose a single image until 0.015). Studio product photography is trivially separable; real people in real clothes are the hard positives. The worst worn outfit scores **−0.0203**, i.e. below 493 of the 507 non-garment photos — consistent with the previously noted single worn outfit at −0.036. Any threshold that buys a lower false-accept rate is paid for **entirely by real users photographing real outfits**, which is exactly the population the gate exists to serve. <br><br>**Hardest themes** (mean margin, least negative first): kitchenware −0.0371, interiors −0.0391, vehicles −0.0443, documents −0.0462, electronics −0.0479. Easiest: plants −0.0724, screens −0.0705, nature −0.0691. Indoor human-made object scenes are the confusable ones; landscapes and screenshots are not. The three highest-scoring surviving negatives are instructive rather than errors: a laptop **displaying a photo of someone's jeans and shoes** (+0.0240), and **graffiti of a figure in armour** (+0.0213) — the gate is right that clothing is depicted, and a pixel-level gate cannot distinguish depicted-clothing from present-clothing. <br><br>**Caveats.** Negatives are CC/public-domain photos from Wikimedia Commons (275) and Openverse (232), which skew toward competent photography — not the badly-lit, motion-blurred frames a phone actually produces, so this is likely still optimistic about image quality even though it is honest about subject matter. 13 collected images were dropped after visual review for containing prominent clothing, including one genuine mislabel the gate itself surfaced: a pair of handmade leather shoes returned by a leatherworking query, which is a positive, not a negative. |
| 2026-08-04 | Phase 9, **brand evidence as an OPEN-SET signal** (gap analysis 11.3c) | `open_set_brand_evidence.py` + `open_set_brand_eval.py`, easyocr + rapidfuzz over an 80-brand gazetteer, catalog vocabulary restricted to the deployed 6 brands | 480 real catalog images, 40 per brand: **240 in-catalog** (adidas/gap/newbalance/nike/pacsun/skechers) vs **240 genuinely off-catalog** (carhartt/champion/dickies/levis/stussy/vans -- real products the 6-brand deployment does not stock, so no synthetic "unknown product" is needed) | — | — | — | — | — | **Precision 100.00% (22/22), recall 9.17% (22/240), fires on 12.5% of images.** `in_catalog` verdicts are also 100% precise (37/37). <br><br>**This is the first signal that is genuinely ORTHOGONAL to the identity embedding.** Brand-as-ranking failed (+0.10pt) because DINOv3 already knew the brand; brand-as-membership does not ask how close anything looks, it reads what the garment says it is. The DINOv3 open-set path has NO usable operating point (AUROC 0.769: 1% false-reject costs 68% false-accept), so a 100%-precision override that stays silent 87.5% of the time cannot make it worse -- it only ever fires when certain. <br><br>**But 9% recall bounds it hard.** It rescues about 1 in 11 off-catalog queries and nothing else. Per-brand recall splits on **wordmark vs logo**, the same split the ranking experiment found: carhartt 17.5%, dickies 17.5%, vans 12.5%, levis 5.0% (small red tab), champion 2.5% ("C" logo), **stussy 0.0%** (handwritten script). Two independent measurements now point at the same missing component -- a logo detector. <br><br>**One false positive, and it was a design flaw rather than noise:** a Nike shoe printing "AIR JORDAN" was called off-catalog because the gazetteer listed `jordan` as an independent brand. OCR read it correctly; the gazetteer was wrong. The in-catalog-wins-ties rule did not save it, because the shoe never printed "Nike" legibly -- so the fix had to be knowing the relationship (`SUB_BRAND_PARENTS`). Precision was 95.65% before that fix and 100% after. Telling a user the product in their hand is not stocked, confidently, is the failure mode that erodes trust fastest, and any real deployment needs a far larger sub-brand map than the six entries here. <br><br>Caveats: the gazetteer is 80 brands chosen by hand and deliberately much wider than the six held-out ones so it is not an answer key, but a production system would carry thousands and recall would not improve -- the binding constraint is OCR legibility, not vocabulary size. Text-but-no-brand (188 of 480 images) is deliberately NOT treated as off-catalog evidence: most of it is sizes and care labels, and calling it evidence would repeat the category gate's unrecoverable-exclusion mistake. |
| 2026-08-04 | Phase 12.1, **attribute-head ceiling check** (linear probe, not a trained head) | frozen `google/siglip2-base-patch16-384` image embeddings + multinomial logistic probe, 5-fold CV | 700 catalog products, 1 image each; labels are `structured_caption.attributes` primary value per facet | — | — | — | — | — | **Attributes ARE recoverable from the frozen embedding, contradicting the expectation that this was a ceiling.** Probe accuracy vs majority-class, at the widest label space tested (min 3 examples/class): **closure 76.4% vs 18.5% majority (+57.9pt, 22 classes)**, canonical_color 74.3% vs 31.4% (+42.9pt, 8 classes), **fit 64.0% vs 37.0% (+26.9pt, 19 classes)**, material 78.3% vs 64.1% (+14.3pt, 23 classes). <br><br>**The lift GROWS as the label space widens**, which is the opposite of the usual pattern and the reason the first pass understated it. At min 25/class only 2-4 classes survive and a high majority baseline masks the signal (closure looked like +41.2pt over a 58.8% baseline); opening to 22 classes drops the baseline to 18.5% and the probe still reaches 76.4%. `fit` is stable at ~+27pt across every setting. <br><br>Consequence for item 12.1: **worth building.** If a LINEAR probe on frozen features recovers this much, a trained head will do at least as well -- the information is already in the embedding and simply is not being used at query time. This corrects the prior assumption, which cited the 27.20% attribute R@1 (2026-07-29 by-label-kind row) and the suspicion that material is not visually grounded. <br><br>**Two caveats that bound the claim.** (1) The labels are LLM-generated, not ground truth -- this measures "can we reproduce what the captioner said", and `caption_apparel.py` had access to product TEXT, so some labels may encode description rather than appearance and the probe may partly exploit product-identity clustering. Learnable either way, but accuracy against these labels is not accuracy against reality. (2) 700 products, one image each: enough to establish the signal exists, not to size it. |
| 2026-08-05 | Phase 12.1, **attribute heads built and wired into retrieval** | `attribute_heads.py` (1-hidden-layer MLP, 768->512->C, on FROZEN SigLIP2 embeddings dumped once by `modal_app_attribute_embed.py`, A10G, 8,241 images / 2,230 products, both `google/siglip2-base-patch16-384` and the real `finetuned_siglip2_hierarchical_v3/stage2_lastnblocks_best`) | 2,230 catalog products, up to 4 images each; split **by PRODUCT** 70/15/15 (no product's images straddle the split); labels = `structured_caption.attributes` primary value per facet after explicit bucketing | see notes | see notes | — | — | — | **The heads work. Using them at query time does not.** <br><br>**Per-facet test accuracy vs majority-class (v3 encoder, MLP head, product-level holdout, image-level accuracy):** closure **85.3% vs 25.2% (+60.1pt, 10 classes)**, length 84.8% vs 34.3% (+50.5pt, 3 classes), material **81.7% vs 50.0% (+31.8pt, 13 classes)**, canonical_color 69.0% vs 25.0% (+43.9pt, 11 classes), fit 67.1% vs 34.0% (+33.1pt, 6 classes). Product-level (pooling a product's image probabilities) is 2-6pt higher again: material 85.7%, closure 87.4%, color 75.2%. Linear head is 4-11pt worse than the MLP everywhere (material 73.6%, closure 77.6%, fit 58.3%, color 58.7%); frozen BASE embeddings are within ~1pt of v3 on material/fit/closure and 7pt worse on colour, i.e. **the attribute signal is in the PRETRAINED features and the fine-tune added almost nothing to it** -- consistent with the 2026-08-04 probe row, now with a product-level split. `pattern` could not be trained at all: **41 labelled products in the entire catalog**, 10 raw values, no class surviving any threshold. That facet is unmeasured, not negative. <br><br>**Long-tail bucketing, decided and documented rather than silently dropped** (`--report-buckets` prints it): (1) ordered first-match keyword rules collapse spellings and sub-varieties into a visual family -- 116 raw materials -> 20 canonical ('nubuck suede'->suede, 'synthetic/vegan/real leather'->leather, 'cotton twill'->twill), 76 fits -> 20, 56 closures -> 11, inseams bucketed at 20in into short/full; (2) any canonical class under 15 TRAIN examples folds into an explicit `other` class that is scored like any other, **not dropped** -- folding costs 0.7-4.3% of products per facet. The material collapse is lossy in a stated direction: nothing in the pixels separates vegan from cowhide, so this head cannot answer a sustainability question. <br><br>**Retrieval: a clean negative, and it is the fourth one.** Query = a held-out image of a val/test product, gallery = 2,195 product prototypes (mean of the product's remaining images), ranking = SigLIP2 v3 cosine -- **the shortlist stage in isolation, no DINOv3 rerank, so these absolutes are NOT comparable to any Phase 4 row**; only the arms within a run compare. Baseline R@1 **76.71% / 77.42% / 77.66%** across split seeds 13/7/29 (~658 queries each). Reranking the top-50 by adding w * sum-of-facets log P(candidate's label | query image): best delta **+0.15pt, -0.76pt, -0.46pt** by seed -- sign flips, magnitude is 1-5 queries out of ~658. A scale-free variant (flat bonus when the head's argmax equals the candidate's label, ruling out a log-prob calibration artefact): **+0.30pt, 0.00pt, -0.15pt**. Both collapse hard at larger weights (-30 to -57pt at w>=0.01), the same shape as `--patch-rerank`. **Predicted attributes add nothing to ranking at any weight tested.** <br><br>**But the ORACLE arm says the idea is sound and the head is the bottleneck.** Replacing the head's prediction with the query product's own LLM label -- a hypothetical 100%-accurate head -- gives **+8.37 / +8.03 / +8.36 pt R@1** (76.71->85.08, 77.42->85.45, 77.66->86.02) and R@5 to 98.0-98.6%, monotonically increasing in weight and consistent across all three seeds. So this is NOT the `--score-fusion` failure mode (signal actively harmful) nor the `--brand-boost` one (signal correct but redundant with the visual embedding): **the ranking information is real and large, and 67-85% per-facet accuracy is simply not enough of it to survive being summed over five facets.** Caveat on the oracle: query-side and candidate-side labels come from the same captioner, so a rare label combination partly identifies the product, and +8pt should be read as a loose upper bound, not a target. <br><br>**THE LABELS ARE NOT GROUND TRUTH.** Every number above is accuracy at reproducing `caption_apparel.py`, which had access to product TEXT. Some labels encode marketing copy rather than appearance ('regenerative cotton', 'workwear twill', 'vegan'), and a head can score well by learning brand or product identity -- product-level splitting kills the trivial version of that, not the subtle one (a brand's catalog shares both a look and a vocabulary, so 'Carhartt-looking => duck canvas' is learnable without seeing canvas). No human-labelled attribute set exists in this project, so the gap between these numbers and reality is **unmeasured**. <br><br>**Verdict: keep the heads, do not wire them into ranking.** They are worth having as a query-time descriptor (spec 4.6/4.8 text-query metadata, and 297 products have no material label and 1,141 no fit label that a head could fill in) -- but as a retrieval rerank they earn nothing and the flag should not exist until a head is materially more accurate. Cost: one A10G embedding run, ~35 min, ~$0.7; all training and every eval arm is CPU-only on a 25MB .npy. |
| 2026-08-04 | Phase 11.3, **brand evidence (spec §4.5) — logo/OCR path, built and measured for the first time** | `brand_evidence.py` + `brand_evidence_eval.py`, local MPS. easyocr (en) at full resolution, CRAFT thresholds loosened to 0.5/0.3, fuzzy match (rapidfuzz ratio) against the catalog's own 12-brand vocabulary read from `metadata.json`; short aliases require sim ≥90, long ≥80 | **1,186 real catalog images** — 50 products per brand × 2 randomly chosen images, deterministic seed, all 12 brands, ground-truth `brand` from the catalog record | recall **11.13%** | precision **100.00%** | no-text **51.01%** | abstain 88.87% | 132 assertions, **0 wrong** | **The signal is real, perfectly precise, and almost never available.** 132 brand assertions over 1,186 images, zero of them wrong — the confusion matrix is exactly diagonal, no off-diagonal cell is non-empty. Precision stays 100.00% at **every** accept threshold from 0.00 to 0.90, so the threshold has nothing to buy and only recall to lose (11.13% at 0.10 → 4.97% at 0.90); it is set to 0.10, i.e. accept any fuzzy match. <br><br>**The no-text rate is the headline and it bounds everything.** 51.01% of catalog product photos contain no text legible to OCR at all. A further 37.86% contain text that is not a brand — size digits, care-label fragments, "ADJUSTABLE" on a cap strap. Only 11.13% yield a brand. <br><br>**Per-brand recall (n≈100 each): adidas 40%, carhartt 21%, skechers 16%, levis 14%, dickies 12%, vans 9.1%, nike 7.1%, gap 6.7%, champion 4.0%, pacsun 2.0%, newbalance 1.0%, stussy 0.0%.** The spread is not noise and it is not about photo quality — it splits precisely along **wordmark vs. logo**. Adidas prints "adidas" in a legible sans-serif on the tongue and heel, so 40% of its photos are readable. New Balance's mark is a giant **"N"** — OCR fires on 78% of New Balance images and reads digits and stray letters, never the words "New Balance", so recall is 1%. Stüssy's mark is stylised handwritten script: 0/100, not a single hit. Champion's is a small embroidered "C" patch, read as `C` or `L`. <br><br>**That is the mechanism, and it is exactly the distinction spec §4.5 already draws.** §4.5 lists "Visible logo" and "OCR" as two *separate* evidence sources. Only OCR was built here, and the brands it fails on are precisely the ones whose evidence is a logo rather than text. Closing that gap needs a logo detector (a small trained classifier over brand marks), not a better OCR engine or looser thresholds — the text genuinely is not there to read. <br><br>Also confirmed working on **page chrome**, the screenshot case §11.2 cares about: on a mock Carhartt product page, easyocr read the header as `CAFHARTT WIP` at confidence 0.32, and fuzzy matching recovered `carhartt` at similarity 0.92. That is one synthetic image, not a rate — no labelled real-screenshot set exists yet — but it does show the misread-wordmark case the fuzzy layer was built for, and it is why the accept threshold could not be based on OCR confidence. <br><br>Engine choice was forced, not preferred: `paddlepaddle` has **no wheel** for this environment (macOS arm64 / CPython 3.14), so paddleocr could not be installed without building from source. easyocr is pure PyTorch and reuses the repo's existing torch/MPS. Resolution is the dominant quality knob — the same Carhartt jacket reads as nothing at 768px, garbage at 1024px, and `carbartt` at conf 0.91 at 2048px — so `MAX_IMAGE_DIM` is deliberately large. Cost: ~2.6 s/image on MPS, ~52 min for the full run, $0 (no GPU rental). |
| 2026-08-04 | Phase 11.3, **brand evidence wired into retrieval — `--brand-boost`, measured** | `hierarchical_retrieval_pipeline.py --evaluate --top-identity-candidates 150 --brand-boost` on Modal A10G, isolated index dir, `GALLERY_IMAGES_PER_PRODUCT=6`, real SigLIP2 v3 + real fine-tuned DINOv3 supcon checkpoint | Volume catalog: **1,077 products, 1,043 held-out queries** (6 brands: adidas/gap/newbalance/nike/pacsun/skechers). **Baseline arm run in the same session, same index, same queries** — do NOT compare these absolutes to the 59.92% row, that one is the 1,234-product Colab catalog and has more distractors | **65.00% → 65.10%** | 88.02% → 88.02% | 94.06% → 94.06% | 74.72% → 74.79% | 1.0 → 1.0 | **+0.10pt. One query out of 1,043. This is zero.** Ungated, boost weight 0.03. R@5 and R@10 are bit-identical, mean rank 11.43 → 11.42. Gated arm: 48.99% → 48.99%, unchanged to two decimals. <br><br>**The ceiling was then measured directly, and it is the same number.** Re-run at `--brand-boost-weight 1.0` — a bonus 33x the AMBIGUITY_MARGIN, large enough that every same-brand candidate outranks every other, i.e. functionally a brand restriction over the candidate set (safe to do *only* because precision is 100%): **R@1 65.10%, identical to weight 0.03.** R@5 moved 88.02% → 88.11%. So the tiny gain is not the weight being mistuned — **the signal has no ranking information left to give at any weight.** <br><br>**Why: brand is nearly redundant with visual identity here.** Brand evidence fired on **11.22%** of queries with **100.00% precision** (independent confirmation of the detection row above, on a different image set — the held-out query views rather than that eval's sample). But on those queries DINOv3 had already put a same-brand product first almost every time. Products of one brand look like each other; a fine-tuned identity embedding has therefore already absorbed most of what the wordmark would tell you. The signal is correct, it is just not *new*. <br><br>**Verdict: keep `--brand-boost` off by default. It is not harmful — unlike `--patch-rerank` (−30pt) and `--score-fusion` (−6.2pt), it costs nothing and breaks no ranking, which is what the boost-not-filter design bought — but it earns nothing either.** It stays in the tree as a measured, off-by-default flag with this row as the reason, not as an unvalidated option. <br><br>The two remaining places brand evidence could still pay, neither tested here: (1) **screenshots**, where the brand is in page chrome rather than on the garment and the OCR recall should be far higher than 11% — needs a labelled real-screenshot set that does not exist yet; (2) **open-set rejection**, where a brand read that matches *no* catalog brand is positive evidence the product is off-catalog — that is a different question from ranking and the 0.769-AUROC row above shows how badly a better signal is needed there. <br><br>Cost: 3 Modal A10G runs (baseline / weight 0.03 / weight 1.0), ~1 hour total, ~$1.2. OCR of 1,043 query images on the GPU is a few minutes and is cached per process across both gate arms. |
| 2026-08-04 | Phase 8, garment gate, **synthetic-negative control** | same script, `--negatives synthetic` (400 generated bar charts, solid colour fields, lorem-ipsum text blocks), same 600 positives | same positives, 400 synthetic negatives | — | — | — | — | — | **AUROC 0.9939 — the synthetic control scores WORSE than the real negatives (0.9994), and the reported 1.0000 could not be reproduced.** The original synthetic set is not in this repo, so 1.0000 is not reproducible here and this row is a re-implementation, not a re-run — the discrepancy may be generator differences rather than a correction. What is measurable: synthetic negatives sit at mean **−0.0175** versus real negatives' **−0.0545**, i.e. **charts and colour fields are three times CLOSER to the garment side than a photo of a sofa is**. 66.25% of synthetic negatives score above the lowest positive, against 6.31% for real ones. <br><br>**So the premise of this exercise was wrong in an interesting way.** Synthetic negatives were assumed optimistic; they are actually the harder set for this particular margin score. The mechanism is visible in the prompt list: the non-garment prompts name *real object categories* ("a photo of furniture", "a photo of an animal"), so a real sofa matches one of them strongly and lands far from the garment side, while a bar chart matches none of them well and drifts toward zero from both directions. The synthetic set was not testing photo-vs-chart as assumed — it was testing an out-of-distribution image against a prompt bank that has no word for it. <br><br>**This does not retire the concern, it relocates it.** The real risk to this gate was never the negatives; it is the positive tail — worn outfits scoring below almost every negative (see the row above). Neither negative set was ever going to reveal that, and only the real positives did. |

## Next rows to fill in

- Phase 1 v3 loss ablation: rerun with `LOSS_TYPE = "infonce"` to check whether multi-positive InfoNCE beats the sigmoid loss used in the row above
- ~~Phase 1 v4 by-facet breakdown~~ — done 2026-08-01, see the row above. Verdict: not worth it, gains too marginal to justify the exact-label regression.
- Phase 1 v5 candidate: less aggressive "model" kind weight cut than v4's, and/or wire in `canonical_color` (from `build_color_hierarchy.py`) as the color facet's training/eval target instead of raw scraped color strings
- ~~Phase 4, `--top-identity-candidates` sweep past 25~~ — **CLOSED 2026-08-03 at K=150.** The K=75 note below predicted no ceiling; K=100 and K=150 found it. Best ungated R@1 **53.95%** at K=150, shortlist miss **1.18%**. Marginal return per candidate collapsed 0.111 (50→75) → 0.0136 (75→100) → 0.0016 (100→150), so K=150 is the practical stopping point and larger K is wasted compute. **The shortlist stage is solved; the bottleneck is now the DINOv3 reranker**, whose conditional R@1 (given the true product is in the shortlist) is flat-to-declining across the sweep and sits at just 54.6%. Next Phase 4 work should target rerank quality, not retrieval breadth. Earlier verdict, kept for history: kept helping with NO sign of diminishing returns through K=75 (best ungated R@1 then **53.53%**, shortlist miss 4.87%).
- ~~Phase 4, HSC climbing real eval~~ — done 2026-08-01, see the two rows above. Verdict: HSC did NOT fix the gate — gated R@1 (36.72%) is now worse than even the old flat gate's (39.66%), despite a real exclusion-rate improvement (25.55% vs 30.00%), because the default HSC_THRESHOLD=0.5 backs off to no restriction (root) 51.9% of the time and to broad "group" restriction 34.9% of the time — only 13.2% of queries get the tight single-category restriction that actually helps. Ungated (47.56%) remains the best real number, a third independent confirmation gating is net-negative. Next: either tune HSC_THRESHOLD much lower and re-test, or accept gating isn't worth pursuing further and keep ungated as the permanent default.
- `build_color_index.py` pixel-color pipeline, v1 (21 canonical colors, "silver" as its own bucket): primary pixel color matched the independent text-based `canonical_color` signal 39.2% of products, primary-or-secondary 56.1%. Diagnosed by inspecting real mismatches (not guessing): merging silver/gray/cream/black into one tolerant "neutral family" for evaluation purposes alone jumped the match rate to 65.0%, showing nearly half the apparent errors were boundary confusion within the near-neutral spectrum (shadows/highlights on white or gray garments landing just past the silver/gray boundary), not real color-family mistakes.
- `build_color_index.py` pixel-color pipeline, **v2 (real fix, not a validation tolerance)**: merged "silver" into "gray" as one canonical bucket everywhere — `build_color_hierarchy.py`'s text-side mapping *and* `build_color_index.py`'s pixel-side reference swatches (20 canonical colors now, was 21) — since average-pixel-color extraction can't distinguish true metallic sheen (needs specular highlight info) from a shadowed neutral gray in the first place, so the extra bucket was pure boundary noise. Re-validated after the change, same methodology as before: primary match **39.2% → 46.4%**, primary-or-secondary **56.1% → 64.8%**. Real, measured improvement, not the evaluation-tolerance trick used to diagnose it.
- Phase 4 rerun with the real fine-tuned DINOv3 checkpoint once it's pushed to the Modal Volume (`modal volume put`) — current 14.96% best number used frozen DINOv3 for the rerank stage
- Phase 4 improvement candidates, in priority order: (1) real DINOv3 checkpoint, (2) drop or soften the category gate (currently net-negative, -3.93pt R@1), (3) widen `TOP_IDENTITY_CANDIDATES` past 10 to attack the 41.90% identity-shortlist miss rate
- Phase 3 step 1: frozen DINOv3 held-out R@K, full 6-brand catalog (`run_dinov3_baseline.py`) — superseded by the frozen-baseline row above; not worth running separately now that Phase 3 step 2 is complete
- Phase 4: `hierarchical_retrieval_pipeline.py` end-to-end eval — **both fine-tunes are now done** (SigLIP2 v3 18.83%, DINOv3 identity 56.55%), so a real accuracy number is possible now, pending pulling the DINOv3 checkpoint from Colab Drive and the SigLIP2 checkpoint from the Modal Volume onto one `DATASET_ROOT`
- **Free-text search (2026-08-01, implemented; full writeup in `docs/roadmap.md`)**: MaskCLIP-style dense/patch-level matching landed in `free_text_visual_search.py` as `--dense-rerank` — pooled-vector search still does the cheap full-catalog pre-filter, dense per-patch matching reranks just that shortlist. Adapted the original MaskCLIP recipe (built for CLIP's CLS-token architecture) to SigLIP2's actual `SiglipMultiheadAttentionPoolingHead` instead: skip the probe's query/key softmax, run every patch through the same value-projection → out_proj → residual LayerNorm+MLP path the pooled output goes through. Smoke-tested locally against the base model only (no fine-tuned checkpoint present in this dev environment) — confirms the mechanism produces a genuinely different ranking than pooled-only search. **Not yet validated against the actual ceiling** — needs the same before/after methodology as `research_localized_query_validation.py`'s 3 real localized test cases, run wherever the real v3 checkpoint is reachable (Modal/Colab). Also still untested: whether v4's checkpoint helps this attribute-heavy task.
- **Exact-retrieval scaling (2026-08-01, full writeup in `docs/roadmap.md`)**: no benchmark number here, a design gap — the pipeline currently always returns a top-1 even when the true product isn't in the catalog at all. Add an open-set rejection threshold (absolute cosine cutoff, or sharper nearest/second-nearest distance-ratio test) so it can report "no confident match," then wire `color_similarity_search.py`/`free_text_visual_search.py` as the explicit fallback when rejection fires. Not implemented yet.
- **Champion added (2026-08-01, no benchmark number yet)**: 200 new records (8 brands, 1,468 total in the catalog now) via `champion_scraper.py`, all 200 targeted variants reached with no category shortfall. Structured-captioned and garment-cropped. Not yet embedded/evaluated against either encoder — SigLIP2 v3/v4 and the DINOv3 identity fine-tune both predate this data, so none of the existing eval numbers reflect it. Re-embedding the catalog (forward-pass only, no retrain needed per the "enrollment" argument in `docs/roadmap.md`) is the next step before Champion shows up in any retrieval number.
- **Levi's added (2026-08-02, no benchmark number yet)**: 178 new records (9 brands, 1,646 total in the catalog now) via `levis_scraper.py` — Jeans 50/50, Jean Jackets 50/50, Shirts 50/50, Accessories 28/50 (Accessories capped by real listing-page coverage, possibly a real catalog-size limit like PacSun/Gap's Sweaters — not confirmed either way, see `SCRAPING_PROCESS.md`'s Levi's section). Structured-captioned (178/178) and garment-cropped. Same "not yet embedded/evaluated" caveat as Champion above — both new brands need a re-embed pass before showing up in any retrieval number.
- **Composed image+text query v1 (2026-08-02, `composed_query_search.py`), informal facet-parser check, NOT a real benchmark**: 15 hand-written text fragments (e.g. "with cargo jorts", "with a puffer jacket", "with black loafers"), each with my own hand-judged expected taxonomy category, checked against `parse_text_fragment`'s real output. **15/15 matched** after two real fixes made while testing: (1) plural nouns ("loafers") weren't matching their singular taxonomy vocab term ("loafer") until word-boundary matching allowed an optional trailing "s"; (2) "khaki pants"/"striped polo" needed two more synonym-table entries (khaki→khakis, polo→polo sweater) to reach the actual taxonomy leaf. No held-out set, one person's own examples and judgment calls — informal, not a rigorous benchmark, reported honestly as such. `second_item_matches` (the `catalog_query_search.py`-side half) was also spot-checked against real catalog output, e.g. "with cargo jorts" correctly surfaced a real product literally named "...Baggy Jorts...". **Correction (2026-08-02, same day, run by a different session)**: the "semantic fallback unavailable, torch 2.0.0 too old" claim above was an environment-configuration issue, not a real tooling limitation — the agent that ran this check didn't activate this repo's `.venv` (torch 2.12.1/transformers 5.12.1, fully working). Re-ran two real queries with `.venv` activated: "with a distressed denim jacket" (canonical category match found, 6/6 real Gap denim jackets returned via lexical match, semantic path not triggered since canonical coverage was sufficient) and "with embroidered lettering stitched across the back panel" (genuinely novel phrasing, zero canonical category match — parser correctly found only loose attribute keywords — semantic fallback engaged for real this time, returned 6 real scored results, ~0.087-0.09 similarity, base SigLIP2 model since no fine-tuned checkpoint is present locally). **The semantic-embedding fallback path is confirmed working in this dev environment when `.venv` is activated.** The `identified_item` half (`hierarchical_retrieval_pipeline.py`'s DINOv3 exact-image stage) remains genuinely blocked locally — confirmed real, not an environment issue — DINOv3 is a gated HuggingFace repo and this environment has no `HF_TOKEN` configured; still needs validation on Colab/Modal.
- **Carhartt WIP added (2026-08-02, no benchmark number yet)**: 200 new records (10 brands, ~1,846 total in the catalog now depending what else landed concurrently) via `carhartt_scraper.py`, all 200 targeted variants reached with no category shortfall (Jackets and Coats/Pants/Shirts/T-Shirts and Polos, 50/50/50/50). Structured-captioned (200/200, $0.067 real Azure cost). Garment cropping correctly skipped for this brand — real, direct visual inspection confirmed Carhartt's product photos are already clean flat-lay shots with no model/background clutter, same reasoning already established for the original shoe photos. Also the first real-world validation that the same-session `pocket_type`/`distressing`/etc. schema addition to `newLLMprompt.py` actually gets populated by the LLM in practice (`"pocket_type": ["side welt"]` on the very first captioned record, from real "side pockets" language in scraped details) — not just present in the schema unused. Same "not yet embedded/evaluated against either encoder" caveat as Champion/Levi's above.
- **Free-text dense-rerank, real-checkpoint validation (2026-08-02, `dense_rerank_real_checkpoint_validation.py`)**: the actual v3 SigLIP2 checkpoint (pulled from the Modal Volume onto this local dev environment via `modal volume get`, a data-transfer op, no GPU compute) used for the first time to test the MaskCLIP-style dense-rerank fix (`free_text_visual_search.py --dense-rerank`, built 2026-08-01, never run against a real checkpoint until now). 3 real localized queries (natural paraphrases, one reused from `research_localized_query_validation.py`, two newly built from real `defining_features` entries with genuinely spatial locations — back neckline, interior lining), against a 777-product local index (only brands with real local image files: Gap/Champion/Levi's/Carhartt/Stussy — not the full catalog). **Genuinely mixed result, honestly reported, not a clear win**: query 1 (denim jacket, button tabs at rear hem) had a pooled rank of 109/777 — outside the dense-rerank shortlist window (`shortlist_k=50`), so dense rerank couldn't even attempt it, a real, structural limitation of the shortlist-then-rerank design when the pooled pre-filter itself misses badly. Query 2 (crewneck, tonal logo at back neckline) got WORSE with dense rerank: pooled rank 22 → dense rank 36 within the same 50-candidate window (-14). Query 3 (trucker jacket, sherpa lining) improved dramatically: pooled rank 27 → dense rank 1 (+26). **1 improved, 1 regressed, 1 unrescuable, at n=3** — not enough samples to call this validated as a net win, and the one regression is real evidence it isn't a strict improvement either. Needs a larger, more systematic test (ideally the same held-out methodology as the main pipeline eval, not 3 hand-picked cases) before trusting `--dense-rerank` as a default.
- **Expanded to 10 cases (2026-08-02, same day)**: 7 more real localized queries added (same methodology — genuine paraphrase, real `defining_features` entries, spatially localized, no generic front/chest placements), against an 872-product local index (catalog grew with Stüssy's addition mid-session). **Final result: 4 improved, 4 worse, 1 unchanged, 1 unrescuable (outside the pooled top-50).** Essentially a coin flip, not a real win. One case is a genuine warning sign, not just noise: "a hat with visible stitching along the curved brim" was already ranked **#1** (perfect) by the pooled vector alone, and dense-rerank made it WORSE (28th/50) — this isn't just "doesn't reliably help," it can actively damage an already-correct result. **Recommendation: do not enable `--dense-rerank` by default as currently implemented.** If revisited, the finding above suggests only applying it when the pooled rank is already poor (e.g. pooled rank > 5-10) rather than unconditionally reranking every query's top-50, since it seems to hurt queries the pooled vector already got right more than it helps queries the pooled vector got wrong.
- **`identified_item` (DINOv3 exact-image stage) run end to end locally for the first time (2026-08-02, later same day)**: `HF_TOKEN` added to this repo's `.env`, resolving the gated-repo blocker flagged in the row above. Ran `composed_query_search.py --image <real product photo> --text "with white sneakers"`: `identified_item` correctly returned the query photo's own product back. **Not an accuracy result** — no fine-tuned DINOv3 checkpoint exists in this dev environment (frozen base model used instead), and the query image was almost certainly already in the searched gallery (no held-out split applied), so this confirms the code path runs correctly without crashing, not real discrimination accuracy. `second_item_matches` for the same query correctly surfaced real white sneakers. Status: "never run, unknown if it even works" → "runs correctly, mechanism confirmed, real accuracy still needs the fine-tuned checkpoint on Colab/Modal."
- **Stüssy added (2026-08-02, no benchmark number yet)**: 175 new records (11 brands, ~2,021 total in the catalog now depending what else landed concurrently) via `stussy_scraper.py` — Tees 50/50, Pants 50/50, Headwear 50/50, Hoodies 25/25 (real catalog-size cap, confirmed via a `products.json` count check before scraping even started, not a scraping shortfall). Structured-captioned (175/175, $0.0555 real Azure cost). Garment cropping correctly skipped — verified by direct visual inspection across all 3 non-capped categories (not assumed from one) that Stüssy's product photos are already clean flat-lay shots, same judgment call as Carhartt. Same "not yet embedded/evaluated against either encoder" caveat as every other brand added this session.
- **Dickies added (2026-08-03, no benchmark number yet)**: 200 new records (13 brands, 2,387 total in the catalog) via `dickies_scraper.py` — Pants 50/50, Shirts 50/50, Shorts 50/50, Coats and Jackets 50/50, no shortfall. Structured-captioned (200/200). Garment cropping run for real (real on-model lifestyle photos, confirmed by direct image inspection, same as Vans) — launched via `segment_apparel.py --brand dickies`, self-checkpointing in the background; check `cropped_images` coverage before assuming it finished. The scraping fork that produced this data stalled partway through the cropping step (session interruption, not a data problem — 200/200 scrape+caption were already complete and safely committed before the stall), picked up and finished directly in this session instead of re-delegating.
- **`unseen_product_enrollment_eval.py`: real bug found and fixed, but the resulting number is NOT a real answer to the enrollment question (2026-08-02, later same day)**: first-ever run (`--unseen-brands stussy,carhartt --control-brand gap`) reported 0 products for every brand — traced to a real bug: `CATALOG` stores brand strings through `display_brand()` (title-cased, e.g. "Gap") but the script filtered against metadata.json's raw lowercase convention ("gap") with an exact-match lookup. **Fixed with case-insensitive matching** (both sides `.lower()`'d before comparison). Re-ran after the fix and confirmed it now works mechanically: 375 unseen products found, 119 control, real R@1 numbers computed (unseen 11.73%, control 20.51%, +8.78pt gap). **This specific number must NOT be read as a real answer to the enrollment claim.** This run used the FROZEN (never fine-tuned) DINOv3 model — no training happened on ANY brand in this run, so the script's own "trained on" vs "never trained on" framing is void here; the +8.78pt gap almost certainly reflects arbitrary differences between the brand groups (photo style, product visual diversity, catalog composition) rather than anything about generalization to unseen identities. What this run actually validates: the bug is fixed and the mechanism produces correct, non-zero, real numbers. The genuine enrollment question — does the REAL fine-tuned DINOv3 checkpoint generalize to brands it never trained on — still needs this script run against that checkpoint (Colab/Modal), not this frozen-model smoke test.
- **Vans added (2026-08-02, no benchmark number yet)**: 200 new records (12 brands, ~2,221 total in the catalog now depending what else landed concurrently) via `vans_scraper.py` — Shoes 100/100, Hoodies and Jackets 50/50, Shirts 50/50, no shortfall. Structured-captioned (200/200, $0.0696 real Azure cost). Garment cropping run for real on the two clothing categories (not skipped, unlike the last two brand additions) — direct image inspection found Vans's photos are genuinely mixed flat-lay/on-model, not uniformly one or the other, so this needed the real crop step (`segment_apparel.py --brand vans`, cropping job launched, self-checkpointing into `metadata.json` in the background — check `cropped_images` coverage on Vans's Hoodies-and-Jackets/Shirts records before assuming it finished). Shoes correctly excluded from cropping entirely (not in `CATEGORY_LABELS`, matching the original 4 shoe brands' convention). First brand added specifically to grow footwear diversity rather than clothing coverage — every other brand added this session was apparel-only. Same "not yet embedded/evaluated against either encoder" caveat as every other brand added this session.
- **Open-set rejection split built, NOT yet run (2026-08-03, `hierarchical_retrieval_pipeline.py --open-set-holdout-fraction`)**: spec §8.1's named open-set split, previously listed as never built. `--reject-threshold` was already implemented but permanently uncalibrated (`REJECT_SIMILARITY_THRESHOLD = None`) because the existing `reject_threshold_sweep` can only measure **false-reject** rate — every eval query's true product is in the gallery by construction, so **false-accept** rate (off-catalog queries the system still asserts a product for) was unmeasurable, and a one-sided error table can't calibrate a threshold. New flag holds out a deterministic fraction of catalog **identities** (not product codes — colorway siblings must go together or the query isn't genuinely off-catalog) from both the DINOv3 gallery and the SigLIP2 shortlist, then reports AUROC (threshold-free separability) plus a two-sided threshold table (false-reject AND false-accept per row, with a `best_balanced` Youden's-J row). Validated locally at the pure-function level only (no GPU): AUROC correct on 4 hand-computable cases + empty guard; split at fraction 0.1 gives 93/930 identities → 124 codes → 698 off-catalog queries, deterministic, zero leakage into gallery or known-query set, and every surviving product keeps a byte-identical split vs. a normal run (non-perturbation verified). **Suggested first real run: `--evaluate --open-set-holdout-fraction 0.1 --top-identity-candidates 75`. Its R@K MUST be logged as its own row, never compared to the rows above — the gallery is deliberately smaller in an open-set run. AUROC and the threshold table are the real outputs.** If AUROC ≈ 0.5, that is a genuine negative finding (the DINOv3 score carries no catalog-membership information and no threshold can work), not a bug.

## NEW BASELINE BLOCK — 12-brand catalog (2026-08-04)

**Rows below this line are NOT comparable to any row above it.** The Modal
Volume was synced from 6 brands / 1,077 gallery products to 12 brands /
2,230, so every query now competes against roughly twice the distractors.
A lower R@1 here is arithmetic, not regression. This is the same discipline
the open-set rows follow, and it was flagged in advance in
`docs/product_gap_analysis.md`.

| Date | Phase | Model | Dataset scope | R@1 | R@5 | R@10 | MRR | Median rank | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 2026-08-05 | Phase 12.2, **fixed-size gallery eval — the catalog-growth confound, resolved** | `--evaluate --top-identity-candidates 400`, `EVAL_GALLERY_SIZE=1000`, Modal A10G, isolated index dir | 12-brand catalog restricted to a deterministic 1,000-product sample (seed 20260805); 787 identities; 985 of 2,195 queries answerable | **58.58%** | 81.12% | 86.70% | — | — | **The apparent 12.27pt collapse from doubling the catalog was ~1.34pt of model difference and the rest was gallery size.** <br><br>Comparison at comparable gallery size: 6-brand / 1,077 products / K=150 gave **59.92%** (miss 1.18%, conditional 60.6%); this 12-brand catalog restricted to **1,000** products at K=400 gives **58.58%** (miss **0.20%**, conditional 58.7%). Unrestricted at 2,230 products it reads 47.65%. So the model did not regress when the catalog doubled — R@1 simply is not comparable across catalog sizes, which is what this metric exists to fix. <br><br>**Why it matters for the roadmap**: coverage is a product requirement (the Dickies failure was 100% a missing-product bug, unreachable by any model improvement), so the catalog must grow. Before this, growth and quality were entangled in one number and every future measurement would have inherited the ambiguity. Now growth can proceed while quality stays measurable at fixed N. <br><br>Not a perfect A/B — the 6-brand row is K=150 against 1,077 products and this is K=400 against 1,000, with a different brand mix — so read it as "the gap is ~1pt, not ~12pt", not as an exact equivalence. Expect raw R@1 to keep falling as streetwear densifies (adding 1,000 more pairs of jeans hurts jeans queries specifically); that is the correct trade and is now separable from regression. |
| 2026-08-05 | Phase 12, **removing the shortlist entirely (K=all)**, WITHOUT gate | `--evaluate --top-identity-candidates 99999`, 12-brand catalog, Modal A10G | full 12-brand catalog, 2,230 gallery products, 2,195 held-out queries | 47.38% | 71.12% | 79.54% | — | — | **Shortlist miss 0.00% by construction, and R@1 moved −0.27pt. Coverage was never the bottleneck.** <br><br>Decomposition across the sweep: K=150 → 43.05% (miss 19.95%, conditional 53.78%); K=400 → 47.65% (miss 5.92%, conditional 50.65%); **K=all → 47.38% (miss 0.00%, conditional 47.38%)**. Driving coverage from 94.08% to 100% was *exactly* cancelled by conditional accuracy falling 50.65% → 47.38%. The reranker gets proportionally more confused as it receives more candidates, so widening the candidate set buys nothing. <br><br>**This closes the "widen the shortlist" line of attack permanently**, and it is a stronger statement than the earlier K-sweep verdicts, which only established diminishing returns at a given catalog size. There is no K, at any catalog size, that recovers the ~5% the shortlist was dropping — the loss reappears as rerank error. <br><br>**It also makes an ANN index premature for accuracy or latency.** Measured cost of the dense scan at current scale: **0.108 ms/query** over 2,230 products, versus a measured **~890–1,180 ms** encoder forward — search is 0.01% of query latency. Extrapolated: 1.17 ms at 50k products, 11.4 ms at 500k, i.e. ~1% of encoder cost at 200x the current catalog. FAISS/HNSW would optimise something already free. It becomes worth building for memory footprint or for multi-tenant serving, not for this. |
| 2026-08-04 | Phase 12.2, **12-brand re-baseline**, WITH HSC gate | `hierarchical_retrieval_pipeline.py --evaluate --top-identity-candidates 150`, `GALLERY_IMAGES_PER_PRODUCT=6`, Modal A10G, isolated index dir | full 12-brand catalog, 2,230 gallery products, 2,195 held-out queries | 32.07% | 46.33% | 50.66% | 38.28% | 9.0 | Gate exclusion **39.00%**, up from 25.80% on the 6-brand catalog — the gate degrades with catalog size too. Shortlist miss 44.46%. Gate net-negative for a seventh time. |
| 2026-08-04 | Phase 12.2, 12-brand re-baseline, WITHOUT gate | same run | same split | **43.05%** | 64.51% | 70.71% | 52.39% | 2.0 | **This is the new headline number, and it reopens a question I closed this morning.** <br><br>Decomposing the 16.87pt fall from the 6-brand 59.92%: **identity-shortlist miss went 1.18% → 19.95%, a 16.9x increase**, while conditional R@1 (accuracy given the true product IS in the candidate set) fell far less, 60.6% → 53.8%. Holding the reranker at its old conditional rate would still have given only 48.5%, so **the shortlist explains ~11.4pt of the 16.9pt drop and the reranker ~5.5pt**. <br><br>**The "candidates sweep is closed at K=150" conclusion does not transfer.** That verdict was measured on a 1,077-product gallery, where K=150 covered ~14% of the catalog and marginal return had collapsed to 0.0016 pt/candidate. On 2,230 products the same K covers ~6.7%, and the shortlist is decisively the bottleneck again. K must scale with catalog size; it is not an absolute. Re-sweeping K on the 12-brand catalog is now the highest-value open experiment, and the earlier "do not push K past 150" entry should be read as catalog-specific rather than settled. <br><br>Practical note: this run is also the first eval on a catalog that includes the six brands the deployed API previously lacked — the failure where a Dickies photo was confidently named as a Nike product was a direct consequence of that gap, not a model error. |
| 2026-08-05 | Phase 11.3, **logo detector — brand recognition WITHOUT reading text** (`logo_detector.py`) | Multinomial logistic probe over **frozen** `google/siglip2-base-patch16-384` image features, local MPS, $0. Six crops per image (full + 4 quadrants + centre), trained crop-level with the image's brand label (weak/MIL supervision), pooled at inference; `--pool full/mean/max` all reported. Split **by product identity** `(brand, slug)` — colourway siblings travel together — hash-deterministic, 719 train / 273 test identities, zero leakage asserted in code | **1,974 catalog images**, 100 product identities per brand × 2 images, all 12 brands; 1,432 train / **542 held-out test** images | acc **99.08%** (full-image) | 98.71% (mean-pool) | 98.34% (max-pool) | macro recall **98.44%** | — | **The classifier works and it does NOT read text — but three controls in the same run say it is not reading logos either.** <br><br>**Per-brand recall vs OCR's (held-out, max-pool): adidas 100% (OCR 40%), carhartt 100% (21%), champion 98.3% (4.0%), dickies 100% (12%), gap 92.5% (6.7%), levis 98.3% (14%), newbalance 100% (1.0%), nike 94.2% (7.1%), pacsun 100% (2.0%), skechers 100% (16%), stussy 100% (0.0%), vans 97.9% (9.1%).** Macro recall 98.44% vs OCR's 11.07%, a 8.9x lift, and the lift is *largest* exactly where OCR failed by construction — Stüssy's handwritten script 0.0% → 100%, New Balance's "N" 1.0% → 100%, Champion's embroidered "C" 4.0% → 98.3%. Precision ≥94.5% on every brand; the confusion matrix has 11 off-diagonal entries out of 542. Abstaining below p=0.5 gives **100.00% precision at 85.42% fire rate** (OCR's shape was 100% precision at 11% fire rate). <br><br>**Control 1 — the crops bought nothing, which is the first warning.** Multi-crop MIL pooling was the entire reason to expect logo reading: a mark is often <2% of the frame and a whole-image 384px embedding averages it away. The **full-image arm is the best arm** (99.08% > 98.71% mean > 98.34% max). A classifier genuinely locating a small mark should *gain* from zooming into regions. This one loses. <br><br>**Control 2 — destroying every mark costs only 15pt.** `--degrade 32` downsamples each image to **32×32** before embedding, at which no logo, wordmark or script of any kind is legible, while colour, silhouette, layout and studio style survive. Accuracy **99.08% → 83.95%**, macro recall 76.72%. Stüssy still scores **82.8% recall at 32×32**, where its script is physically unreadable. **So ~85% of the performance is carried by information that contains no mark at all.** The residual ~15pt is the only part that could be logo detail, and even that is not shown to be marks rather than fabric texture. <br><br>**Control 3 — it collapses off catalog photography, which is the deployment case.** Scored 300 real outfit photos from `outfit_dataset` (Reddit/Pinterest, people wearing clothes) with the catalog-trained probe. Max-class confidence: catalog held-out **mean 0.852 / median 0.911, 95.02% above 0.5**; real photos **mean 0.406 / median 0.389, 24.67% above 0.5**. **AUROC separating real-photo from catalog-photo using confidence alone: 0.9670.** The confidence score is very close to a pure "is this a catalog product shot" detector. <br><br>**Control 4 — background removal is survivable (7 brands, n=231).** Re-scored on `cropped_images` (`*_cropped.jpg` only — the field interleaves originals wherever segmentation declined, and including those would have put as-shot photos back into the control built to remove them): **92.64%**, macro recall 92.96% over the 7 brands present. So it is not *only* studio background — but read with controls 2 and 3, what survives is garment colour palette, cut and photographic treatment, i.e. house style. <br><br>**Verdict: this is a brand-photography-style classifier, not a logo detector.** It is genuinely useful as a *catalog-side* tool (99% accurate brand labelling of catalog imagery for free) and genuinely misleading as a query-side one. Small-n caveats: adidas n=6 and newbalance n=14 test images (those brands have the fewest distinct slugs), so their 100% figures are the least stable in the table. Cost: ~50 min MPS embedding for the main run plus ~25 min for the controls, $0, no GPU rental, no model download — both backbones were already in the HF cache. |
| 2026-08-05 | Phase 11.3, **is brand already implicit in the embedding? (the control `--brand-boost` never had)** — `logo_detector.py --nn-baseline` | Cosine nearest neighbour among the 1,432 training images on the **same frozen SigLIP2 embedding**, brand of the top-1 neighbour taken as the prediction. Identity-retrieval-shaped, no probe, no training | same 542 held-out images, split by product identity | NN brand acc **96.49%** | probe 99.08% | delta **+2.59pt** | — | — | **This measures directly what the 2026-08-04 `--brand-boost` row could only infer.** That row explained its +0.10pt (1 query in 1,043, and *the same 65.10%* at weight 1.0, i.e. a functional brand restriction) by saying "products of one brand look alike, so DINOv3 had already ranked a same-brand product first". Here that claim is a number: **plain nearest-neighbour retrieval already gets the brand right 96.49% of the time**, and a trained 12-way probe on the same features adds 2.59pt. Per-brand NN accuracy: adidas 100%, carhartt 100%, champion 96.7%, dickies 100%, gap 97.5%, levis 94.8%, newbalance 100%, nike 88.5%, pacsun 100%, skechers 98.0%, stussy 92.2%, vans 97.9%. <br><br>**Consequence: a brand BOOST cannot pay, and no retrieval run was spent to re-confirm it.** The 2026-08-04 weight-1.0 arm already measured the ceiling of a *100%-precision* brand signal over the candidate set and it was +0.10pt; the only thing a logo detector changes is how often that signal is available (11% → ~100%). Extrapolating that row's own per-firing gain (1 query out of the 117 that fired) to full coverage bounds the best case at roughly **+0.9pt, and that extrapolation assumes the detector's brand knowledge is information the identity embedding lacks — which 96.49% says it largely is not.** Not wired into `hierarchical_retrieval_pipeline.py`: the expected value is at best sub-point, the file is locked by concurrent work, and a Modal arm measuring a feature already bounded this tightly is not worth the spend. **This is the negative result the task anticipated, and it now has a directly measured cause rather than an inferred one.** |
| 2026-08-05 | Phase 11.3, **logo detector as an OPEN-SET signal** (`logo_detector.py --open-set`) | Probe trained on the **6 deployed brands only** (adidas/gap/newbalance/nike/pacsun/skechers), scored by max-class softmax; off-catalog = the 6 brands the deployment does not stock (carhartt/champion/dickies/levis/stussy/vans), exactly the split the 2026-08-04 OCR open-set row used | 592 train / **208 in-catalog held-out** / **1,174 off-catalog** images | AUROC **0.9092** (full), 0.9098 (mean), 0.8802 (max) | best-balanced J 0.675 @ t=0.834 (17.31% false-reject / 15.16% false-accept) | off-catalog recall at 1% false-reject **20.44%** | at **zero** false-reject: 9.37% | — | **The AUROC beats the DINOv3 distance path (0.9092 vs 0.769) and the operating point that matters is still worse (20.44% vs 32% off-catalog recall at 1% false-reject). Do not quote the AUROC on its own.** <br><br>**Two things make the comparison not like-for-like, both in this signal's favour, so the real gap is worse than it looks.** (1) This split holds out entire **brands**; the 0.769 row holds out **identities within known brands**, which is a strictly harder question. (2) Per control 3 in the row above, max-class confidence separates real-outfit photos from catalog photos at **AUROC 0.967** — higher than the 0.909 it achieves at separating off-catalog brands. **The rejection signal is mostly a domain detector.** A real user's phone photo of an in-catalog garment scores like an off-catalog product (mean confidence 0.406 vs 0.852), so deployed against real queries this would false-reject heavily, which is the failure mode that erodes trust fastest and the reason nothing here is wired in. <br><br>The one genuinely orthogonal open-set signal on record remains OCR's 100% precision / 9.17% recall — it fires rarely but it reads what the garment *says*, which no amount of studio-style similarity can fake. Zero-false-reject recall here (9.37%) happens to land on the same number as OCR's 9.17% recall, but by a completely different and far less trustworthy mechanism. |

---

## 2026-08-06 — SERVING was running K=25, not the measured 150/400

**Found by querying the live API with a catalog image that IS in the
gallery and finding it absent from the top 50.** `num_identity_candidates`
came back 30–344 against a 3,522-product gallery.

`TOP_IDENTITY_CANDIDATES = 25` is a module constant in
`hierarchical_retrieval_pipeline.py`. `retrieve()` accepts it as a kwarg
and `--evaluate` passes it explicitly, which is why every eval row used
150 or 400 — but `modal_app_serve.py` never passed it, so **the deployed
API had always shortlisted 25 identities out of 1,562 (1.6% of the
space)**. Every headline number in this log was produced at a K the
service did not use.

Fixed: the constant is now env-overridable (default unchanged at 25, so
evals and scripts are unaffected) and `modal_app_serve.py` sets
`TOP_IDENTITY_CANDIDATES=400`.

| | before (K=25) | after (K=400) |
|---|---:|---:|
| identity candidates considered | 30–344 | 1,679–1,902 |
| the 8 spot-check products | 0 in top-50 for 3 of them | rank 1, 1, 1, 2, 2, 5 for 6 of them |

**Sanity measurement after the fix**, 150 random catalog images, 18-brand
/ 3,522-product gallery, query image IS in the gallery (so this is
SELF-retrieval and is NOT comparable to the 47.65% held-out row — it is
strictly easier and exists only to check the serving path is sane):

| metric | value |
|---|---:|
| exact product R@1 | **65.3%** (98/150) |
| exact product R@5 | 78.0% |
| exact product R@10 | 80.7% |
| brand top-1 | **84.0%** |

That self-retrieval sits at 65% rather than ~100% is the identity
encoder confusing visually near-identical products across brands (plain
tees, work pants) — consistent with the 47.65% held-out figure, not a
new fault.

**K is still not tuned for this catalog.** 400 was measured at 2,230
products; the gallery is now 3,522 and K scales with catalog size. 400 is
a floor, not a tuned value. Latency does not constrain the sweep: the
dense scan is 0.108 ms/query against a ~890–1180 ms encoder forward.

---

## 2026-08-18 — outfit search gets its FIRST evaluation (VLM-judged)

Outfit search had no evaluation of any kind. Every number in this log
measures item identification; none said whether "red sweater" returns red
sweaters. `outfit_search_eval.py` fixes that without needing a human:
gpt-4o-mini vision (the project's existing Azure Foundry deployment) labels
outfit photos with the garments and colours it sees, and queries are scored
against those labels.

**938 photos labelled**, 847 (90%) judged to be real worn-outfit photos.
2 API failures. 4.8 minutes, 8 workers.

**precision@15, colour+garment queries, 20 queries, ~275 judged results:**

| arm | micro | macro |
|---|---:|---:|
| `type_preference=0` (no type/colour binding) | 40.9% | 39.2% |
| **`type_preference=0.05` (shipped)** | **46.0%** | **44.0%** |
| | **+5.1pt** | **+4.8pt** |

So the structural binding added on 2026-08-17 — parse a garment group,
category and colour out of the text and reward the crop that satisfies all
of them — is worth **+5.1pt micro precision**, measured rather than
eyeballed. It was previously justified only by crop-level counts
("yellow shoes" 2/20 → 13/20) and by looking at results.

**Method notes, both of which matter for reading the number:**

- Results are ranked **within the judged pool** (retrieve 3,000 deep, keep
  the top k that the judge saw). A plain top-20 gave 1–5 judged results per
  query, because the labelled pool is ~9% of the corpus — precisions of
  0/1 and 3/3, and an aggregate that was noise. Ranking within the judged
  pool measures ORDERING rather than label coverage. It also makes the task
  easier than production, so the ABSOLUTE value is optimistic; comparisons
  across arms remain valid, which is what this is for.
- **One model grading another, not human ground truth.** The judge is
  independent of the retrieval stack — no shared weights or training data
  with SigLIP2 or the ATR parser — which is what makes disagreement
  informative rather than circular. But VLMs miscall colours under coloured
  lighting and confuse sweater/sweatshirt/hoodie exactly as this pipeline
  does. Trust the delta more than the level.

**Weakest queries** (`black t-shirt` 7%, `white socks` 0%, `white t-shirt`
20%) point at the same place: black and white garments are where the
detector's own colour naming is least reliable, and socks are barely
visible in a full-body frame.
