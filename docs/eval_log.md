# Evaluation log

Running record of every experiment run for the iteration loop described in
`docs/roadmap.md`. One row per run. Numbers only — if you want the reasoning
behind a change, that's in `roadmap.md` or the relevant commit, not here.

Metrics follow spec §8.2: Recall@1/5/10, MRR, median/mean rank. Caption
retrieval runs (SigLIP2) also report num_candidate_captions since R@5/R@10
are only meaningful relative to how many candidates exist.

| Date | Phase | Model | Dataset scope | R@1 | R@5 | R@10 | MRR | Median rank | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 2026-07-27 | Phase 1 smoke test | finetuned (srpone/zooclaw-fashionsiglip2) | 12-product local sample, 58 images, 7 candidate captions | 75.86% | 100% | 100% | 87.36% | 1.0 | Pipeline validation only, not a real baseline — too few candidates for R@5/10 to be meaningful |
| 2026-07-27 | Phase 1 smoke test | base (google/siglip2-base-patch16-384) | 12-product local sample, 58 images, 7 candidate captions | 68.97% | 100% | 100% | 80.80% | 1.0 | Same caveat — pipeline check, finetuned > base directionally sane |
| 2026-07-28 | Phase 1 real baseline | finetuned (srpone/zooclaw-fashionsiglip2) | full 1115-product / 6-brand catalog, 4334 images, 561 candidate captions | 7.89% | 21.87% | 31.15% | 16.09% | 26.0 | Real baseline — R@1 much lower than the smoke test's 76%, expected since 561 real candidates vs. 7 toy ones. This is the number that matters. |
| 2026-07-28 | Phase 1 real baseline | base (google/siglip2-base-patch16-384) | full 1115-product / 6-brand catalog, 4334 images, 561 candidate captions | 7.15% | 18.39% | 25.73% | 13.69% | 40.0 | Finetuned beats base on every metric (R@1 +0.7pt, R@5 +3.5pt, R@10 +5.4pt, MRR +2.4pt, median rank 26 vs 40) — the finetune is real, not noise, but overall retrieval is still weak in absolute terms (R@1 under 8%) with 561 candidates. Confirms Phase 1's own conclusion: worth re-training on the full corpus (roadmap Phase 1 next step), current finetune was only ever trained on the old 2-brand shoe set. |
| 2026-07-29 | Phase 1 full-corpus retrain (v2) | finetuned SigLIP2 (`finetune_siglip2.py`, two-stage: heads then last-block, structured_caption-only, no SKU text, category-scoped hard-negative batching) | full 1234-product / 6-brand / 13-category catalog, exact-label test split, category-scoped candidate pool | 14.04% | 36.83% | 53.86% | 26.40% | 9.0 (mean 15.26) | First real full-corpus retrain, run directly (not Modal). Nearly 2x the prior baseline's R@1 (14.04% vs 7.89%) and a much tighter median rank (9 vs 26) — the finetune-on-full-corpus hypothesis from the 2026-07-28 baseline row is confirmed. This is now the number `finetune_siglip2_v3.py` is trying to beat. |
| 2026-07-29 | Phase 1 v3 improvement round | finetuned SigLIP2 (`finetune_siglip2_v3.py` on Modal, A10G: last-4-block unfreeze w/ layer-wise LR decay, offline mined hard negatives, all-positive-caption sampling, sigmoid loss, K=4/batch 64) | full 1234-product catalog, exact-label test split, global candidate pool | 15.83% | 43.47% | 57.99% | 29.33% | 8.0 (mean 17.54) | Global-pool number, for direct comparison to the 2026-07-28 baseline row (which was global-pool only). |
| 2026-07-29 | Phase 1 v3 improvement round | finetuned SigLIP2 (`finetune_siglip2_v3.py` on Modal, A10G, same config as above) | full 1234-product catalog, exact-label test split, category-scoped candidate pool | 18.83% | 51.09% | 67.59% | 34.12% | 5.0 (mean 10.94) | Beats v2 on every metric (R@1 +4.8pt, R@5 +14.3pt, R@10 +13.7pt, MRR +7.7pt, median rank 5 vs 9). Selected checkpoint: stage2_lastnblocks (val R@1 14.79% vs stage1's 10.51%). Ablations not yet isolated (sigmoid loss only was tried; InfoNCE and per-lever contributions untested) but the combined v3 change set is a clear win over v2. |

| 2026-07-29 | Phase 1 v3, by label kind | finetuned SigLIP2 (`finetune_siglip2_v3.py` checkpoint, stage2_lastnblocks_best) via `evaluate_siglip2_by_label_kind_modal_body.py` | full 1234-product catalog, same 2,059-image held-out test split as the row above, kind=**generic** (category, 276 candidates) | 95.09% | 100% | 100% | 97.34% | 1.0 | Breaks the 18.83% exact_label number down by label kind, per user request ("18.83% seems low, captions are simple/feature-oriented — what's the R@1 per kind"). `exact_label` in the training script only ever evaluates the "model" kind below — this row shows coarse category classification is essentially solved. |
| 2026-07-29 | Phase 1 v3, by label kind | same checkpoint | same split, kind=**brand** (e.g. "Nike sneaker", 581 candidates) | 61.39% | 91.26% | 97.38% | 74.07% | 1.0 | Solid. |
| 2026-07-29 | Phase 1 v3, by label kind | same checkpoint | same split, kind=**attribute** (e.g. "black leather sneaker", 2,633 candidates) | 27.20% | 68.53% | 84.36% | 45.18% | 3.0 | Weak — large fine-grained candidate space, and some attribute facets (material especially) may not be strongly visually grounded, so part of this could be a real ceiling rather than pure undertraining. |
| 2026-07-29 | Phase 1 v3, by label kind | same checkpoint | same split, kind=**model** (specific product line, 1,264 candidates) | 17.68% | 45.90% | 61.15% | 31.41% | 6.0 | This is what the 18.83% headline number actually measures — the hardest tier by design (one step below full SKU, which is deliberately DINOv3's job, not SigLIP2's). Weakest of the four, and the one dragging the aggregate down. |

| 2026-07-29 | Phase 3 step 2 | frozen DINOv3 backbone (raw pooled features, no head) | full 1234-product catalog, exact-SKU validation split | 25.24% | 41.39% | 50.75% | 33.36% | 10.0 (mean 140.62) | Frozen baseline, printed before stage 1 training — 16.3% of wrong top-1s were same-model/different-colorway (the "reasonable" confusion), 83.7% unrelated item. |
| 2026-07-29 | Phase 3 step 2 | DINOv3 identity fine-tune, stage1_head (`dino_identity_finetune.py`, SupCon loss, P×K=8×4 identity-balanced batches, colorway-sibling hard negatives) — epoch 10/10, heads-only | full 1234-product catalog, exact-SKU validation split | 56.84% | 76.35% | 84.82% | 65.79% | 1.0 (mean 21.14) | Huge jump over the frozen baseline (R@1 +31.6pt) from training a projection head alone. 9.8% of wrong top-1s same-model/different-colorway vs. 90.2% unrelated — the ambiguous-confusion share barely moved, meaning the gains are from real discrimination, not just getting better at the easy case. Stage 2 (last-block unfreeze) crashed immediately after on a real bug (see roadmap) — this is the stage1 checkpoint's number, not a final test-set result yet. |
| 2026-07-30 | Phase 3 step 2 | DINOv3 identity fine-tune, stage2_lastblock (`dino_identity_finetune.py`, rerun after the `model.model.layer` fix, epoch 6/6 val) | full 1234-product catalog, exact-SKU validation split | 57.28% | 76.96% | 84.91% | 66.20% | 1.0 (mean 20.71) | Only +0.44pt val R@1 over stage1's 56.84% — last-block unfreezing barely moved the needle beyond what head-only training already captured. Selected as final checkpoint anyway per the "beats stage1" rule. |
| 2026-07-30 | Phase 3 step 2 | DINOv3 identity fine-tune, **final held-out test** (stage2_lastblock_best) | full 1234-product catalog, exact-SKU test split | **56.55%** | 76.72% | 84.96% | 65.60% | 1.0 (mean 22.99) | **Phase 3 complete.** Up from a 25.24% frozen baseline (+31.3pt). 13.2% of wrong top-1s are same-model/different-colorway vs. 86.8% unrelated — most remaining errors are real misses, not the "reasonable" confusion case, room left for hard-negative refinement but this is a strong result as-is. |
| 2026-07-30 | Phase 4, first real run | `hierarchical_retrieval_pipeline.py` end-to-end (`--evaluate`) on Modal — SigLIP2 v3 stage2_lastnblocks_best + **frozen (non-fine-tuned) DINOv3** (checkpoint not yet on the Modal Volume, only on Colab Drive), WITH category gate | 1,077-product catalog (some missing images on this Volume), 1,043 held-out queries | 11.03% | 21.48% | 30.39% | 16.95% | 69.0 (mean 542.29) | First real (not mechanical-only) Phase 4 number. Category-gate exclusion rate 27.52% (over a quarter of queries get gated into the wrong category, unrecoverable) — the gate is net-negative, see the row below. Identity-shortlist miss rate 49.66% — the SigLIP2 semantic-narrowing stage (top-10 identity candidates) is the dominant bottleneck, not the gate and not even the DINOv3 rerank. |
| 2026-07-30 | Phase 4, first real run | same run, WITHOUT category gate (fallback comparison) | same split | **14.96%** | 27.61% | 37.97% | 21.88% | 27.0 (mean 458.62) | Beats the gated config on every metric — confirms the gate is currently net-negative, not just noise. Identity-shortlist miss rate still 41.90% even ungated — this is the real bottleneck to attack next (widen `TOP_IDENTITY_CANDIDATES` beyond 10, and/or v3→v4's "model" kind quality, which v4 deliberately trimmed sampling weight on — worth checking this doesn't regress once v4 lands). This whole row is still artificially low from frozen DINOv3 doing the final rerank instead of the real 56.55% fine-tuned checkpoint — rerun once that's pushed to the Modal Volume for the real number. |
| 2026-07-30 | Phase 4, **real run** (both fine-tuned checkpoints) | `hierarchical_retrieval_pipeline.py` end-to-end (`--evaluate`) on Colab — SigLIP2 v3 stage2_lastnblocks_best + **real fine-tuned DINOv3** (56.55%-R@1 checkpoint, pushed from the Modal Volume down to Colab Drive), WITH category gate | full 1234-product catalog, 1,190 held-out queries | 31.34% | 45.80% | 47.98% | 37.39% | 1235.0 (mean 637.15) | Big jump from the frozen-DINOv3 run (R@1 11.03%→31.34%) — DINOv3 fine-tuning was the single biggest lever, as predicted. Category-gate exclusion rate 30.00%, identity-shortlist miss rate 51.51% (>50%, which is why median rank lands on the sentinel value — see next row's note). Median rank at this "just over 50% miss" threshold is a real artifact of the metric, not a new problem. |
| 2026-07-30 | Phase 4, real run | same run, WITHOUT category gate (fallback comparison) | same split | **36.72%** | 53.19% | 56.05% | 43.69% | 4.0 (mean 536.65) | Beats gated on every metric again — gate confirmed net-negative a second time, independent run. Identity-shortlist miss rate 43.36% (just under 50%, hence the low median here vs. 1235 above — same underlying miss-rate distribution, different side of a threshold). **Conditional R@1** (accuracy given the true product IS in the shortlist): with-gate 64.6%, without-gate 64.8% — essentially identical, meaning DINOv3's rerank itself is excellent (~65%) and the category gate has zero effect on rerank quality, only on shortlist coverage (where it still hurts). Confirms the identity-shortlist stage (SigLIP2 top-10 narrowing), not DINOv3, is now the dominant bottleneck by a wide margin — same conclusion as the frozen-DINOv3 run, unchanged in magnitude (43-52% miss both times). |
| 2026-07-31 | Phase 4, **after widening shortlist** | same checkpoints, `TOP_IDENTITY_CANDIDATES` 10→25 (per the diagnosis above), gate still on by config default at the time of this run (pre-dates the gate-off-by-default code change) | same 1234-product catalog, 1,190 held-out queries, WITH category gate | **39.66%** | 59.75% | 62.77% | 47.67% | 3.0 (mean 436.43) | Category-gate exclusion rate unchanged (30.00%, expected — the gate logic itself didn't change). Identity-shortlist miss rate **35.21%, down from 51.51%** — widening the shortlist worked exactly as predicted. |
| 2026-07-31 | Phase 4, after widening shortlist | same run, WITHOUT category gate | same split | **47.65%** | 72.52% | 76.72% | 57.79% | 2.0 (mean 249.08) | **Best result yet.** Identity-shortlist miss rate **20.00%, down from 43.36%** — more than halved. R@1 up 10.9pt from the pre-widening run (36.72%→47.65%). Conditional R@1 (given shortlist hit) actually dipped slightly, 64.8%→59.6% — makes sense: DINOv3 now discriminates among 25 candidates instead of 10, a harder per-candidate task, but the massive drop in outright misses more than compensates. Net win is unambiguous. |
| 2026-07-30 | Phase 1 v4 improvement round | finetuned SigLIP2 (`finetune_siglip2_v4.py` on Modal, A10G: per-facet `LABEL_KIND_WEIGHTS` — color/fit/closure boosted, "model" kind cut 0.42→0.30 to make room) | full 1234-product catalog, exact-label test split, category-scoped candidate pool | 16.00% | 45.78% | 63.35% | 30.55% | 6.0 (mean 12.46) | **Regression vs. v3 on every metric** (R@1 -2.83pt, R@5 -5.31pt, R@10 -4.24pt, MRR -3.57pt). Confirms the predicted tradeoff: cutting "model" kind's sampling weight to fund color/fit/closure hurt the exact-label task (which *is* essentially the "model" kind) more than expected. `hierarchical_retrieval_pipeline.py`'s checkpoint auto-detection doesn't include v4, so this doesn't affect Phase 4 — still using v3. By-facet breakdown on v4 not yet run, so whether color/fit/closure actually improved enough to justify trying a less aggressive v5 reweighting is still open. |
| 2026-08-01 | Phase 1 v4 by-facet breakdown (the open question above, now answered) | `evaluate_siglip2_v4_by_facet_modal_body.py` on Modal, A10G, vs. the existing v3 by-facet numbers already on the Volume | same 1234-product catalog / split as the v3 by-facet rows above | color 33.85% (v3 31.57%, **+2.29pt**) | material 48.50% (v3 50.08%, -1.58pt) | fit 21.18% (v3 20.05%, **+1.13pt**) | closure 24.09% (v3 23.45%, **+0.64pt**) | **Verdict: not worth it.** The 3 facets v4 specifically boosted (color/fit/closure) all improved, but only marginally — the biggest gain is +2.29pt (color), the other two are ~+1pt or less. `length` unchanged (37.50%→37.50%), `defining_features` and `attribute_caption` both flat/slightly worse. None of this comes close to justifying the exact-label ("model" kind) regression of -2.83pt from the same row above — v4 traded a real, large loss on the hardest/most important task for a thin spread of small gains on secondary facets. Confirms v3 should stay the production checkpoint; if a v5 is attempted, it needs a much gentler "model" kind weight cut than v4's 0.42→0.30, not just abandoning the idea. |

| 2026-08-01 | Phase 4, **real HSC climbing eval** (the question flagged as highest priority since 2026-08-01's HSC-climbing code change) | `hierarchical_retrieval_pipeline.py --evaluate` on Colab — SigLIP2 v3 + real fine-tuned DINOv3, same checkpoints as the 07-31 rows, WITH HSC-based category gate (HSC_THRESHOLD=0.5 default) | full 1234-product catalog, 1,190 held-out queries | 36.72% | 56.22% | 59.41% | 44.62% | 3.0 (mean 474.81) | **HSC did NOT fix the gate — it's now WORSE than the old flat gate on R@1** (36.72% vs. the 07-31 flat gate's 39.66%, -2.94pt), despite genuinely improving the category-gate exclusion rate (25.55% vs. flat gate's 30.00%, a real -4.45pt improvement). Diagnosis from `hsc_climb_level_fractions`: leaf 0.0%, category 13.2%, group 34.9%, root 51.9% — at the default threshold, HSC backs off to NO restriction (root) over half the time and to a broad multi-category "group" restriction another third of the time; only 13.2% of queries get the tight single-category restriction that would actually help the identity-shortlist stage. Isolating just the non-excluded queries confirms the narrowing that DOES land at category level genuinely helps (17.15% shortlist-miss rate vs. ungated's 20% — computed by backing the 25.55%-exclusion queries out of the aggregate 38.32% shortlist-miss rate), but that thin benefit only applies to 13.2% of queries, nowhere near enough to offset the cost of the 25.55% still excluded outright. Root cause is almost certainly the threshold being too conservative for this use case, not the climbing algorithm itself being wrong. |
| 2026-08-01 | Phase 4, real HSC eval | same run, WITHOUT category gate (fallback comparison) | same split | **47.56%** | 72.52% | 76.64% | 57.73% | 2.0 (mean 249.09) | Matches the 07-31 ungated baseline almost exactly (47.65%→47.56%, noise-level), confirming HSC climbing itself changed nothing about the ungated path (expected — HSC only feeds `allowed_categories`, which ungated mode ignores). **Ungated remains the best real number, a third independent confirmation that category gating (flat OR HSC) is net-negative** — `retrieve()`'s gate-off-by-default (set 2026-07-30) is confirmed correct, not just a hedge. If gating is revisited at all, HSC_THRESHOLD needs to be tuned much lower first (to climb to leaf/category more often instead of defaulting to group/root) before it has a chance of beating ungated, and even then it isn't guaranteed to — the identity-shortlist stage may just be strong enough on its own that any exclusion cost outweighs any narrowing benefit. |

## Next rows to fill in

- Phase 1 v3 loss ablation: rerun with `LOSS_TYPE = "infonce"` to check whether multi-positive InfoNCE beats the sigmoid loss used in the row above
- ~~Phase 1 v4 by-facet breakdown~~ — done 2026-08-01, see the row above. Verdict: not worth it, gains too marginal to justify the exact-label regression.
- Phase 1 v5 candidate: less aggressive "model" kind weight cut than v4's, and/or wire in `canonical_color` (from `build_color_hierarchy.py`) as the color facet's training/eval target instead of raw scraped color strings
- Phase 4 next: `--evaluate` mode always runs both gated and ungated explicitly regardless of `retrieve()`'s default, so the 47.65% row above is already the real current best number — the gate-off-by-default code change (2026-07-30) only matters for `--image` single-query/production use, not this benchmark. Shortlist miss rate is down to 20% (from 51%) but not zero — could push `TOP_IDENTITY_CANDIDATES` higher than 25 and see where returns diminish
- ~~Phase 4, HSC climbing real eval~~ — done 2026-08-01, see the two rows above. Verdict: HSC did NOT fix the gate — gated R@1 (36.72%) is now worse than even the old flat gate's (39.66%), despite a real exclusion-rate improvement (25.55% vs 30.00%), because the default HSC_THRESHOLD=0.5 backs off to no restriction (root) 51.9% of the time and to broad "group" restriction 34.9% of the time — only 13.2% of queries get the tight single-category restriction that actually helps. Ungated (47.56%) remains the best real number, a third independent confirmation gating is net-negative. Next: either tune HSC_THRESHOLD much lower and re-test, or accept gating isn't worth pursuing further and keep ungated as the permanent default.
- `build_color_index.py` pixel-color pipeline, v1 (21 canonical colors, "silver" as its own bucket): primary pixel color matched the independent text-based `canonical_color` signal 39.2% of products, primary-or-secondary 56.1%. Diagnosed by inspecting real mismatches (not guessing): merging silver/gray/cream/black into one tolerant "neutral family" for evaluation purposes alone jumped the match rate to 65.0%, showing nearly half the apparent errors were boundary confusion within the near-neutral spectrum (shadows/highlights on white or gray garments landing just past the silver/gray boundary), not real color-family mistakes.
- `build_color_index.py` pixel-color pipeline, **v2 (real fix, not a validation tolerance)**: merged "silver" into "gray" as one canonical bucket everywhere — `build_color_hierarchy.py`'s text-side mapping *and* `build_color_index.py`'s pixel-side reference swatches (20 canonical colors now, was 21) — since average-pixel-color extraction can't distinguish true metallic sheen (needs specular highlight info) from a shadowed neutral gray in the first place, so the extra bucket was pure boundary noise. Re-validated after the change, same methodology as before: primary match **39.2% → 46.4%**, primary-or-secondary **56.1% → 64.8%**. Real, measured improvement, not the evaluation-tolerance trick used to diagnose it.
- Phase 4 rerun with the real fine-tuned DINOv3 checkpoint once it's pushed to the Modal Volume (`modal volume put`) — current 14.96% best number used frozen DINOv3 for the rerank stage
- Phase 4 improvement candidates, in priority order: (1) real DINOv3 checkpoint, (2) drop or soften the category gate (currently net-negative, -3.93pt R@1), (3) widen `TOP_IDENTITY_CANDIDATES` past 10 to attack the 41.90% identity-shortlist miss rate
- Phase 3 step 1: frozen DINOv3 held-out R@K, full 6-brand catalog (`run_dinov3_baseline.py`) — superseded by the frozen-baseline row above; not worth running separately now that Phase 3 step 2 is complete
- Phase 4: `hierarchical_retrieval_pipeline.py` end-to-end eval — **both fine-tunes are now done** (SigLIP2 v3 18.83%, DINOv3 identity 56.55%), so a real accuracy number is possible now, pending pulling the DINOv3 checkpoint from Colab Drive and the SigLIP2 checkpoint from the Modal Volume onto one `DATASET_ROOT`
- **Free-text search (2026-08-01, implemented; full writeup in `docs/roadmap.md`)**: MaskCLIP-style dense/patch-level matching landed in `free_text_visual_search.py` as `--dense-rerank` — pooled-vector search still does the cheap full-catalog pre-filter, dense per-patch matching reranks just that shortlist. Adapted the original MaskCLIP recipe (built for CLIP's CLS-token architecture) to SigLIP2's actual `SiglipMultiheadAttentionPoolingHead` instead: skip the probe's query/key softmax, run every patch through the same value-projection → out_proj → residual LayerNorm+MLP path the pooled output goes through. Smoke-tested locally against the base model only (no fine-tuned checkpoint present in this dev environment) — confirms the mechanism produces a genuinely different ranking than pooled-only search. **Not yet validated against the actual ceiling** — needs the same before/after methodology as `research_localized_query_validation.py`'s 3 real localized test cases, run wherever the real v3 checkpoint is reachable (Modal/Colab). Also still untested: whether v4's checkpoint helps this attribute-heavy task.
- **Exact-retrieval scaling (2026-08-01, full writeup in `docs/roadmap.md`)**: no benchmark number here, a design gap — the pipeline currently always returns a top-1 even when the true product isn't in the catalog at all. Add an open-set rejection threshold (absolute cosine cutoff, or sharper nearest/second-nearest distance-ratio test) so it can report "no confident match," then wire `color_similarity_search.py`/`free_text_visual_search.py` as the explicit fallback when rejection fires. Not implemented yet.
- **Champion added (2026-08-01, no benchmark number yet)**: 200 new records (8 brands, 1,468 total in the catalog now) via `champion_scraper.py`, all 200 targeted variants reached with no category shortfall. Structured-captioned and garment-cropped. Not yet embedded/evaluated against either encoder — SigLIP2 v3/v4 and the DINOv3 identity fine-tune both predate this data, so none of the existing eval numbers reflect it. Re-embedding the catalog (forward-pass only, no retrain needed per the "enrollment" argument in `docs/roadmap.md`) is the next step before Champion shows up in any retrieval number.
- **Levi's added (2026-08-02, no benchmark number yet)**: 178 new records (9 brands, 1,646 total in the catalog now) via `levis_scraper.py` — Jeans 50/50, Jean Jackets 50/50, Shirts 50/50, Accessories 28/50 (Accessories capped by real listing-page coverage, possibly a real catalog-size limit like PacSun/Gap's Sweaters — not confirmed either way, see `SCRAPING_PROCESS.md`'s Levi's section). Structured-captioned (178/178) and garment-cropped. Same "not yet embedded/evaluated" caveat as Champion above — both new brands need a re-embed pass before showing up in any retrieval number.
- **Composed image+text query v1 (2026-08-02, `composed_query_search.py`), informal facet-parser check, NOT a real benchmark**: 15 hand-written text fragments (e.g. "with cargo jorts", "with a puffer jacket", "with black loafers"), each with my own hand-judged expected taxonomy category, checked against `parse_text_fragment`'s real output. **15/15 matched** after two real fixes made while testing: (1) plural nouns ("loafers") weren't matching their singular taxonomy vocab term ("loafer") until word-boundary matching allowed an optional trailing "s"; (2) "khaki pants"/"striped polo" needed two more synonym-table entries (khaki→khakis, polo→polo sweater) to reach the actual taxonomy leaf. No held-out set, one person's own examples and judgment calls — informal, not a rigorous benchmark, reported honestly as such. `second_item_matches` (the `catalog_query_search.py`-side half) was also spot-checked against real catalog output, e.g. "with cargo jorts" correctly surfaced a real product literally named "...Baggy Jorts...". **Correction (2026-08-02, same day, run by a different session)**: the "semantic fallback unavailable, torch 2.0.0 too old" claim above was an environment-configuration issue, not a real tooling limitation — the agent that ran this check didn't activate this repo's `.venv` (torch 2.12.1/transformers 5.12.1, fully working). Re-ran two real queries with `.venv` activated: "with a distressed denim jacket" (canonical category match found, 6/6 real Gap denim jackets returned via lexical match, semantic path not triggered since canonical coverage was sufficient) and "with embroidered lettering stitched across the back panel" (genuinely novel phrasing, zero canonical category match — parser correctly found only loose attribute keywords — semantic fallback engaged for real this time, returned 6 real scored results, ~0.087-0.09 similarity, base SigLIP2 model since no fine-tuned checkpoint is present locally). **The semantic-embedding fallback path is confirmed working in this dev environment when `.venv` is activated.** The `identified_item` half (`hierarchical_retrieval_pipeline.py`'s DINOv3 exact-image stage) remains genuinely blocked locally — confirmed real, not an environment issue — DINOv3 is a gated HuggingFace repo and this environment has no `HF_TOKEN` configured; still needs validation on Colab/Modal.
- **Carhartt WIP added (2026-08-02, no benchmark number yet)**: 200 new records (10 brands, ~1,846 total in the catalog now depending what else landed concurrently) via `carhartt_scraper.py`, all 200 targeted variants reached with no category shortfall (Jackets and Coats/Pants/Shirts/T-Shirts and Polos, 50/50/50/50). Structured-captioned (200/200, $0.067 real Azure cost). Garment cropping correctly skipped for this brand — real, direct visual inspection confirmed Carhartt's product photos are already clean flat-lay shots with no model/background clutter, same reasoning already established for the original shoe photos. Also the first real-world validation that the same-session `pocket_type`/`distressing`/etc. schema addition to `newLLMprompt.py` actually gets populated by the LLM in practice (`"pocket_type": ["side welt"]` on the very first captioned record, from real "side pockets" language in scraped details) — not just present in the schema unused. Same "not yet embedded/evaluated against either encoder" caveat as Champion/Levi's above.
